# Build-context hygiene: keep credentials, logs, and build artifacts out of
# the Docker build context (only COPYed paths land in the image, but the
# context itself is sent to the daemon and scanned by secret scanners).
.env
.env.*
logs/
target/
dependency-reduced-pom.xml
*.log
.git/
README.md
CLAUDE.md
