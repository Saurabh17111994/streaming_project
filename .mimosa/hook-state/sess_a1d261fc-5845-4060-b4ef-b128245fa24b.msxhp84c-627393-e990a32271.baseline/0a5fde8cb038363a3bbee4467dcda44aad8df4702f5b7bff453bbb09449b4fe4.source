package com.trading.common.model;

/** Order lifecycle states. Keyed by broker_order_id. See DEC-013. */
public enum OrderLifecycleState {
    SUBMITTING,
    PENDING,
    PARTIAL,
    FILLED,
    CANCELLED,
    REJECTED,
    UNKNOWN
}
