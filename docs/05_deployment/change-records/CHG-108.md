# CHG-108 — test-count truth bump compute 387→388 (2026-08-26 live re-verify)

2026-08-26 `mvn test` per module re-verify found a one-test drift from the
CHG-102 truth: compute suite now at **388 (was 387)**. Common/ingestion
unchanged (466/247). The bump reflects one additional compute test now
present in surefire (new `DedupRocksDbThroughputMemoryIT` ground, 2026-08-26
perf-probe addition — the class was added earlier and now runs green). This
record re-pins the machine-gate truth to the live run and carries the future
3-socket note in `03-ingestion.md` as a docs-only diagnosis section.

```text
change_record_id: CHG-108
scope: gate-behavior
owner: saurabh
date: 2026-08-26
affected_artifacts: code/01_platform/04_scripts/stale_table_kind_scan.py (TEST_COUNT_TRUTH/C6_TRIPLE_TRUTH/SUITE_TRIPLE_TRUTH compute 387→388 + history comment); docs/08_implementation/01-foundation.md (L3 banner 466/247/387→466/247/388); docs/08_implementation/03-ingestion.md (deferred 3-connection / 3000-instrument future plan section + status triple 466/247/387→466/247/388)
compatibility_class: COMPATIBLE
savepoint_impact: none — docs + gate-truth constants only; no runtime topology, state, or DDL change
test_updates: compute 387→388 (+1 compute test — DedupRocksDbThroughputMemoryIT live-probe addition now in surefire 388/0/22, BUILD SUCCESS); common 466/0/2 and ingestion 247/0/8 unchanged (live re-verify 2026-08-26); no test removals
rollback_behavior: revert the constants in stale_table_kind_scan.py + the banner in 01-foundation.md + the deferred 3-connection section in 03-ingestion.md; audit truth reverts to 466/247/387; no runtime impact
plan_tasks: 03-ingestion.md
```

## Change

1. **Test-count truth re-pin compute 387→388** — `mvn test` per module on
   2026-08-26: **common 466/0/2, ingestion 247/0/8, compute 388/0/22**
   (BUILD SUCCESS). Prior truth 466/247/387 (CHG-102, 2026-08-25) is now
   466/247/388. Updated `stale_table_kind_scan.py`
   (`TEST_COUNT_TRUTH`, `C6_TRIPLE_TRUTH`, `SUITE_TRIPLE_TRUTH` compute
   slot 387→388), `01-foundation.md` L3 banner (same), and the status echo in
   `03-ingestion.md`. No other dossier carries the live triple.

2. **Deferred 3-connection plan (docs only)** — added a diagnosis-only
   `### Future: 3-connection / 3000-instrument scaling plan` section to
   `03-ingestion.md` (broker-burst note → future plan → Fingerprint
   contract). The section records that 60k/ticks ingestion already has the
   multi-socket machinery (`MaxHFTConnections=3`, `ARROW_HFT_CONNECTIONS`
   1..3, `Slots` hft-0/1/2, `runHFTSupervisor` multi-slot) and blocks only on
   the deliberate `len(plan.Slots)!=1` FATAL guard; all sockets funnel to one
   `RawTickWriter`/`raw_table_1`. No code/config/schema change — marked
   `deferred — NOT to be implemented now`.

## Verification

| Check | Result |
| --- | --- |
| `mvn test` — common / ingestion / compute | 466/0/2 / 247/0/8 / 388/0/22 — BUILD SUCCESS (2026-08-26) |
| `python -m docs_audit` (C6) | surefire vs doc 466/247/388 — green |
| `python stale_table_kind_scan.py` | no UNANNOTATED stale claims (dossiers) |
| `python stale_table_kind_scan.py --ddl` | no DDL/manifest drift |

## Discipline

C6 truth constants must be bumped on every test add/remove — keep surefire
(BLOCK: flink-performance diagnosis-only) gated on `01-foundation.md:3`.
