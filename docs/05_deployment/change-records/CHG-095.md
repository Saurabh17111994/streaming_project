# CHG-095 — Secrets-rotation drill rehearsal (MinIO S3 credential class)

Rehearsal of `docs/05_deployment/04-secrets-rotation.md` rotation + revocation
against the dev MinIO endpoint (172-dev stack, `127.0.0.1:9000`), credential
class **S3/lake** (runbook table row "S3 checkpoint/lake"). No production
credential touched; no service restarted (drill is restart-free by design);
order path stays `HALTED`.

```text
change_record_id: CHG-095
scope: topology-secret
owner: saurabh
date: 2026-08-24
affected_artifacts: docs/05_deployment/04-secrets-rotation.md (rehearsed procedure, no doc change), code/01_platform/01_docker/.env (read-only evidence: EXECUTION_ENABLED=false, no secret changed)
compatibility_class: NOT_APPLICABLE
savepoint_impact: none
test_updates: none — drill-only rehearsal; validation performed via live mc commands recorded in logs/phase-d-20260824T151952Z/01-secrets-rotation.md
rollback_behavior: drill credentials are removed after validation (recorded in evidence); MinIO standing state restored (root credentials unchanged, no service restarted)
plan_tasks: live-readiness Phase D #7 (secrets rotation drill)
```

## What was rotated

A dedicated MinIO IAM user (drill-scoped, no service consumer), created for
the drill:

1. Version 1 credential minted + validated (bucket access works).
2. Version 2 credential minted while Version 1 still valid -> **overlap
   window**: both versions valid simultaneously (planned-rotation acceptance).
3. Version 1 revoked -> validation that the revoked credential is **rejected**.
4. Version 2 validated post-rotation (recovery after rotation).
5. No leakage: secrets never printed; evidence records fingerprints only.
6. Order path not enabled by rotation: `EXECUTION_ENABLED=false` verified
   before and after; MinIO S3 role cannot authorize orders.
7. Drill credentials removed (rollback of the rehearsal artifact); root
   MinIO credentials and the lake tier untouched (no restart, no env change).

Evidence: `logs/phase-d-20260824T151952Z/01-secrets-rotation.md`.
