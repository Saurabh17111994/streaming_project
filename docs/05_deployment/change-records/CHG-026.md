# CHG-026 — execution-core re-architecture: consolidate Action Capture, Babysitter & Executor into one integrated dossier (Nautilus + go-arrow bridge)

```text
change_record_id: CHG-026
scope: docs (implementation dossiers + index + audit tooling paths) — no production code changed
owner: Execution Team · Action Capture Team · Babysitter Team
date: 2026-08-18
affected_artifacts: docs/08_implementation/05-execution-core.md (new — integrated Execution Core dossier), docs/08_implementation/00-start-here.md (dossier map, readiness, implementation order, Phase 4+5), docs/08_implementation/11-testing-and-release.md (acceptance-coverage owner cells for AC-AC/AC-BB/AC-EXE → 05-execution-core.md), docs/01_project/04-decisions.md (DEC-041), code/01_platform/04_scripts/docs_audit.py (C10 COVERAGE_DOSSIERS), code/01_platform/04_scripts/implementation_gate.py (task-6 dossier path), code/02_services/03_action_capture/README.md, code/02_services/04_executor/README.md, code/02_services/02_compute/README.md (dossier links)
compatibility_class: COMPATIBLE
savepoint_impact: none — documentation-only refactor; no state layout, topology, operator, or runtime artifact changes (BabysitterJob / common safety-core code untouched)
test_updates: NO NEW TESTS and no test changes — the compute suite and all module suites are untouched; the only code-file edits are two audit-script path strings (docs_audit.py COVERAGE_DOSSIERS, implementation_gate.py dossier path) so the C10 coverage gate and the order gate resolve the new dossier
rollback_behavior: revert the doc changes and the two script path edits (git history restores 05/06/07 full content at 74f3d89); delete 05-execution-core.md; DEC-041 row removed from the decisions index; no state or runtime impact
plan_tasks: 05-execution-core.md (the integrated Execution Core dossier — status: design revision, upstream re-scope pending), 00-start-here.md (Phase 4+5 merged section)
```

## What landed and why

The user decision (2026-08-18) re-architects the execution half of the platform:

**Nautilus → go-arrow bridge → Arrow.** Nautilus (Rust-native trading engine) becomes the
execution/position core — OMS, position engine, risk engine, reconciliation, fill dedup, and the
event-store audit — replacing three parallel implementations (Action Capture's lifecycle/position
projectors, Babysitter observation, Executor's gate/attempt/audit machinery). The go-arrow bridge
(a localhost service wrapping the already-pinned `go-arrow` SDK) is the **only** component that
talks to Arrow. Custom glue remains: the Fluss trade-row reader, projection sinks to the
`Fills`/`Order_Lifecycle`/`Positions`/`Execution_*` tables, the two-person gate + fencing, and
attempt/correlation mapping.

This change record covers the **documentation refactor only**: the three dossiers are consolidated
into `05-execution-core.md` (each old dossier reduced to a dated SUPERSEDED stub, then deleted
2026-08-18 per user request; full historical content remains in git at `74f3d89`). The audit
tooling, index, coverage table, decision index, and service READMEs are updated so the doc gates
stay green. The upstream build contracts
(`docs/04_contracts/05-babysitter.md`/`06-action-capture.md`/`07-executor.md`), the executor
requirements, and DEC-006 are **not** re-scoped in this change — that re-scope is the recorded
gate before implementation (see `05-execution-core.md` §Open gates and upstream impact). Live
money stays BLOCKED; no broker call path changed or was enabled.
