# CHG-026 — Item E safety-consumer live job run: `SlotAssignment` Serializable fix + guard test + C6 truth bump (340→341)

```text
change_record_id: CHG-026
scope: code (common safety), test, docs (18-signal-job-remaining-work-plan Item E execution)
owner: Compute Team
date: 2026-08-18
affected_artifacts: code/common/src/main/java/com/trading/common/safety/SlotAssignment.java, code/common/src/main/java/com/trading/common/safety/SlotAssignmentResolver.java, code/common/src/test/java/com/trading/common/safety/SlotAssignmentResolverTest.java, docs/08_implementation/04-signal-job.md, docs/08_implementation/01-foundation.md, docs/08_implementation/18-signal-job-remaining-work-plan.md, code/01_platform/04_scripts/stale_table_kind_scan.py, Makefile, logs/tracker-14/safety-live-job-run-20260818.md (gitignored logs/, per convention), logs/tracker-14/SafetyLiveJobRun.java (gitignored host-run runner, per convention)
compatibility_class: COMPATIBLE
savepoint_impact: none — the safety consumer carries no Flink keyed/operator state (the SafetyStateTracker is a per-task in-memory object, the dossier's stated future work), and the Serializable fix adds a marker interface without changing any state layout, topology, or serialized state schema. The live run's checkpoint (chk-3, RETAIN_ON_CANCELLATION) is a dev-run artifact under /tmp/safetyhalt-checkpoints, not a production savepoint
test_updates: common +1 — SlotAssignmentResolverTest.serializableRoundTrip (ObjectOutputStream round-trip of a 1,500-token / 2-slot assignment asserting fingerprint, slot ids, token-set hashes, and slotIdOf lookups all survive serialization); common suite 340 → 341 run / 0 failures / 1 env-gated skip. compute unchanged (292 run / 0 failures / 17 env-gated skips). C6 truth bumped 340/234/292 → 341/234/292 in 01-foundation.md L42; stale-claim scanner truth constants (TEST_COUNT_TRUTH / C6_TRIPLE_TRUTH) + Makefile help text bumped to match; scanner's kind-claim span regex hardened to treat `;` as a clause break (removed a pre-existing false-positive at 09-production-swarm.md:343 where `Signal_Candidates` … `KV` over-matched across two clauses). ITEM E LIVE RUN 2026-08-18 (explicit user approval) — SafetyHaltJob ran end-to-end against the dev Fluss (in-process MiniCluster, topology identical to SafetyHaltJob.main, parallelism 1): UNSAFE → NEW_UNSAFE (suppression) → RECOVERED → RECOVERED (admission) observed through the live consume path in changelog order, safety.transitions.applied incremented, 0 malformed rows from the run's own rows, checkpoint chk-3 retained on clean cancel (RETAIN_ON_CANCELLATION). The run surfaced and fixed a latent submittability defect: SlotAssignmentResolver was not Serializable though carried as a Flink operator field, so the job had never been submittable (NotSerializableException at graph build) — fixed by SlotAssignment extends Serializable + SlotEntry implements Serializable. A first parallelism-16 attempt demonstrated the per-task-tracker subtask-split finding (UNSAFE and RECOVERED landing on different subtasks), recorded as the reason the safety consumer runs at parallelism 1 until the tracker moves to broadcast state
rollback_behavior: revert the two common source edits (git history) to restore the pre-fix interfaces — this re-breaks job submittability exactly as before (the defect was latent, not a regression); delete the guard test to restore the 340 count and revert the C6 truth line + scanner constants + Makefile help. The live-run evidence and host runner live in gitignored logs/ per convention. No state readability impact — no checkpoint or savepoint depends on this change
plan_tasks: 18-signal-job-remaining-work-plan.md (Item E resolved 2026-08-18, E.1–E.6 all ticked), 04-signal-job.md (§Slot-scoped safety consumer "Deferred" bullets annotated DONE 2026-08-18 + the parallelism-1 finding; §DOC-CONSISTENCY live C6 pointer corrected to 341/234/292), 01-foundation.md (C6 truth 341/234/292)
```

## What landed and why

Item E of `18-signal-job-remaining-work-plan.md` was the last
user-approval-gated item: run the `SafetyHaltJob` safety consumer live
against the dev Fluss. The user gave explicit approval on 2026-08-18; the
run executed the same day and PASSED. Live money stayed BLOCKED throughout
(this is the dev-cluster safety-consumer path, no execution involved); the
Executor stayed HALTED; no credential left the gitignored `.env`.

The run's primary value was catching a defect the unit suites could not:

| Finding | Disposition |
| --- | --- |
| `NotSerializableException: SlotAssignmentResolver` at Flink graph build | FIXED — the assignment is a field of the safety consumer's operator, so it crosses the job-graph serialization boundary; `SlotAssignment extends Serializable` + `SlotEntry implements Serializable`. The job had never been submittable before this. |
| Parallelism-16 splits a slot's UNSAFE/RECOVERED across subtasks (per-task tracker) | RECORDED — the safety consumer runs at parallelism 1; the tracker's move to broadcast state stays the dossier's stated future work. Not a tracker bug. |
| Pre-existing malformed row (contract_version=21) in table history | EXPECTED — replayed by `OffsetsInitializer.full()`; the job counted it in `safety.rows.malformed` and skipped it (malformed-never-fatal by design). The run's own rows (contract_version=2) contributed 0 malformed. |

E.4 assertions all held on the live consume path: UNSAFE → `NEW_UNSAFE`
(suppression opens), RECOVERED at epoch+1 → `RECOVERED` (admission),
`safety.transitions.applied` incremented, `safety.rows.malformed` stayed 0
for the run's rows. E.5 held: clean cancel retained checkpoint chk-3
(`RETAIN_ON_CANCELLATION`) with `_metadata` under
`/tmp/safetyhalt-checkpoints/<jobId>/chk-3/`.

Evidence: `logs/tracker-14/safety-live-job-run-20260818.md` (scope/safety
boundary, defect + fix, run mechanism, live pair + E.4 mapping, parallelism
finding, replay-history outcomes, checkpoint, disposition).
