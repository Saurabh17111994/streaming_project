# CHG-098 — Parallel slice batch: J↔R HMAC parity, IMAGES-012 evidence, EOD extend drill

Parallel laptop batch (3 subagents, 2026-08-24) implementing the three
"can-do-now" audit items (document audit C-list). One real code fix + one
real code-parity close + two evidence bundles.

```text
change_record_id: CHG-098
scope: code-fix + evidence
owner: saurabh
date: 2026-08-24
affected_artifacts: code/02_services/04_executor/src/gateway_protocol.rs (+5 parity tests); code/02_services/06_execution_gateway/src/test/java/com/trading/execution/gateway/GatewayProtocolParityTest.java (new, 5 tests); code/common/src/main/java/com/trading/common/schema/eod/EodControllerTool.java (shadow-name fix); code/01_platform/04_scripts/version_matrix.yaml (VM-IMAGES-012 evidence); docs/08_implementation/12-version-compatibility-evidence.md (IMAGES row); docs/08_implementation/05-execution-core.md (gateway-protocol parity note); docs/08_implementation/02-schema-storage.md (retention drill outcome)
compatibility_class: NOT_APPLICABLE
savepoint_impact: none
test_updates: yes — 5 new Java parity tests + 5 new Rust parity tests (see below); no existing test changed
rollback_behavior: revert the 3 source hunks (parity tests, parity test file, shadow-name fix); matrix/doc annotations are evidence-only
plan_tasks: IMAGES-012 library-layer scan (trivy Java DB network-blocked 2026-08-24) + SEC-IMAGE-001 numeric threshold decision; EOD large-table bulk copy for 385k-row rewrite; eod-controller shadow-name fix is in this CHG (tested 62 Eod tests + live apply)
```

## Results per slice

| Slice | Result | Evidence |
| --- | --- | --- |
| Java↔Rust HMAC envelope parity | **CLOSED** — byte-identical canonicalization on deliberately non-alphabetical payload `{"zulu":"z","alpha":"a","qty":100}` (payload_json, sha256 payload_hash, HMAC auth, outer field order equal both ways; cross-verify accepted in both directions). Rust `cargo test --lib gateway_protocol` 14/14 (8 pre-existing + 5 new + 1 config); Java `GatewayProtocolParityTest` 5/5 + `GatewayProtocolTest` 2/2 (regression). preserve_order (Cargo.toml L39) retained. No encode/verify behavior change. | `gateway_protocol.rs`, `GatewayProtocolParityTest.java` |
| IMAGES-012 | **PARTIAL** — 12/12 RepoDigests verified matching versions.pin/.env; buildx SBOM attestations for fluss/flink/zookeeper/golang (openobserve absent); trivy OS-layer counts disclosed (python 50/3 worst, golang 17/2, zookeeper 13/0, fluss 2/0, flink 0/0); library-layer scan UNAVAILABLE (trivy Java DB download network-blocked, 2×300 s); 5 builder images not present locally; SEC-IMAGE-001 numeric threshold TO_BE_VERIFIED → class stays UNKNOWN, `PARTIAL_EVIDENCE_DIGEST_SBOM_OS_ONLY` | `logs/version-matrix/images-012-20260824T171559Z.md` |
| EOD retention extend drill | **PASS small-table / FAIL large-table (honest)** — `Portfolio_Reservations__eod_ext_20260824` created ttl=32 d, rows 1/1 parity, 5 s; `feature_candles_15s` shadow ttl=37 d but copy 1738/385,389 rows → per-row UpsertWriter ~10 rows/s exceeds 180 s drill bound (large-table rewrite not viable yet — finding). **Real bug found+fixed:** shadow name invalid on non-UTC (BASIC_ISO_DATE on ZonedDateTime emitted `+0530`; `toLocalDate()` fix; 62 Eod tests + live apply) | `logs/eod-retention-extend-20260824T171204Z.md` + shadow retained |

## Operational note (not caused by code)

2026-08-24 17:08Z dev-cluster Exit137 (host OOM under 3-parallel-slice load):
all 5 data-plane services killed; SignalJob `5869c40e…` lost.
Recovered: `docker compose up -d` (12/12 healthy) → SignalJob restored from
`chk-1625` (new job `7e9c1e49…`, startup-mode RESTORE, chk-1634 continuing,
9 completed / 0 failed at check time). No data loss; checkpoints served as the
recovery path exactly as designed.

## Discipline

eod_offload_state synthetic drill rows re-scoped to `table_name=cleaned_dummy`
(VERIFIED) after the drill; `make eod-controller ARGS="status"` back to
RESULT=OK EXIT=0, all 10 scoped tables "no days on file". `/tmp` scratch
probes (CleanDummy.java, CheckFinal.java) removed; pi-lens workspace clean.
No git/but operations performed by subagents (main commits).
