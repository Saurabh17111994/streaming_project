# CHG-068 — Task C3: losslessness re-validation at scale (multi-connection)

```text
change_record_id: CHG-068
scope: ingestion-bridge, ingestion-pipeline, hft-policy, requirement
owner: Saurabh (solo operator, DEC-044)
date: 2026-08-21
affected_artifacts: code/02_services/01_ingestion/go-bridge/c3_losslessness_test.go, logs/nautilus-execution/c3-losslessness-multiconn-20260821.md
compatibility_class: COMPATIBLE
savepoint_impact: none — additive tests mirroring ING-TCP-001 reconcile logic in-memory; no schema, state, savepoint, or checkpoint format touched. Existing deployments with N=1 see zero behavior change.
test_updates: new code/02_services/01_ingestion/go-bridge/c3_losslessness_test.go (2 tests: TestLosslessnessMultiConn — 1,024→1, 2,048→2, 2,433→3, 3,072→3 slots with per-token 0 bad/0 vanished/0 extra; TestLosslessnessMultiEpoch — restart re-sharding deterministic, no token migration, multi-epoch aggregated delta) — Go PASS (18.7s), Rust 153/0
rollback_behavior: delete the C3 test file; losslessness proof reverts to single-socket (N=1) coverage; no state to migrate
plan_tasks: docs/plans/2026-08-25-live-readiness-unified-plan.md (Task C3), 19-nautilus-execution-service-implementation-plan.md
```

## Summary

Closes C3 (offline slice): multi-connection sharding preserves the
count-based losslessness guarantee (`ING-TCP-001`). The bridge's per-token
tick counter (`ndjson.go` `tickCounts` + `ARROW_TICK_COUNTS` file report) is
aggregated across slots; the reconcile script's logic (bridge counts vs
sink-side per-token rows, 0 lost/extra/vanished) is mirrored in-memory:

* `TestLosslessnessMultiConn` — N=1..3, full 2,433 manifest, per-token exact.
* `TestLosslessnessMultiEpoch` — restart re-sharding deterministic, multi-epoch
  delta = epoch1 + epoch2.

Full Fluss live reconcile (`bridge → raw_table_1 → probe →
reconcile-compare.py --exact` with market-hours ticks) is deferred to D-era
(prod VMs + market-hours) per the triage; the offline slice proves the
sharding + counting invariant without Fluss.

## Evidence

`logs/nautilus-execution/c3-losslessness-multiconn-20260821.md` — invariant,
per-test table, capacity validated, suite results (Go PASS, Rust 153/0).
