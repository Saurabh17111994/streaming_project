# CHG-021 — SignalChainLiveE2ETest harness fixes ported from the FreeBuff design-B investigation (managed/network memory passthrough, count engine, runner env, stream drains); DEC-038 stays authoritative

```text
change_record_id: CHG-021
scope: config, test-hardening
owner: Compute and Strategy Teams
date: 2026-08-17
affected_artifacts: code/02_services/02_compute/src/main/java/com/trading/compute/signaljob/SignalJob.java, code/02_services/02_compute/src/main/java/com/trading/compute/signaljob/SignalJobConfig.java, code/02_services/02_compute/src/test/java/com/trading/compute/signaljob/RuntimeOptionsTest.java, code/02_services/02_compute/src/test/java/com/trading/compute/signaljob/SignalChainLiveE2ETest.java, code/01_platform/04_scripts/run-signal-chain-e2e.sh, logs/tracker-14/16-signal-chain-e2e-investigation-20260817.md, 04-signal-job.md, 01-foundation.md
compatibility_class: COMPATIBLE
savepoint_impact: none — additive env passthrough keys (TASK_MANAGER_MEMORY_MANAGED_SIZE, TASK_MANAGER_NETWORK_MEMORY_MAX) default to unset, in which case the deployment (flink-conf.yaml) stays authoritative; no DDL, state-layout, serializer, or checkpoint-contract change; production behavior unchanged when the keys are absent
test_updates: RuntimeOptionsTest +3 (managedSizePassthroughApplied, networkMemoryMaxPassthroughApplied, managedSizeBlankRejected) + rocksdbDevDefaults gains the unset-key assertion; SignalChainLiveE2ETest harness hardening (LOG listOffsets count, KV max-of-3 dip-retry count, daemon stream drains, backend/memory env passthrough, 5 s scan poll); compute suite count re-measured in 01-foundation.md C6 line + status banners
rollback_behavior: revert the four code files + runner script and the test additions; with the keys unset the config translation is byte-identical to pre-CHG-021, so the rollback is a pure code revert — no DDL/state migration or live-cluster action
plan_tasks: 04-signal-job.md (P7/P10 verification backlog — E2E harness now reaches the live tail; SIGNAL-CHAIN-E2E-001 registration)
```

> **SUPERSEDED (2026-08-17, CHG-022 / DEC-040):** the design-B rejection below
> was overturned by the smoke-8–16 evidence — the DEC-038 synchronous Fluss
> lookup on the dedup hot path stalls the checkpoint barrier (checkpoint 1
> expired at exactly 30 s, zero acks) and cannot be fixed by harness config.
> Dedup authority now returns to Flink keyed state (Design B); this record's
> history stays intact, only the decision line is superseded. See
> `docs/05_deployment/change-records/CHG-022.md`.

## What landed

The FreeBuff worktree `0526f7dc` investigation (`16-signal-chain-e2e-investigation.md`,
492 lines, 2026-08-17) root-caused the `SignalChainLiveE2ETest` failure to a
**harness config gap, not a design bug**: under local/embedded execution (no
`flink-conf.yaml`) Flink defaults `taskmanager.memory.managed.size` to
**128 MB TOTAL**, starving the RocksDB block cache so the job throttles to ≈ the
feed rate, the backlog never drains, and every candle is a KV upsert of a
historical window — `feature_candles_15s` stays flat. Same topology sustains
~100k/s on heap and ~49k/s on RocksDB with a realistic managed budget.

The same investigation also proposed a **design-B (state-authoritative
MapState dedup)** architecture reversal. That was **rejected** (user decision
2026-08-17): DEC-038 stays canonical — Fluss owns the durable dedup set; the
Flink side keeps the bounded working cache. Design B is preserved for reference
on the `experiment/design-b-mapstate-dedup` branch (`30baa10`) and never merged.
**Superseded by CHG-022 the same day** — see the banner above. Only the
architecture-independent harness fixes were ported:

| Piece | Detail |
| --- | --- |
| `TASK_MANAGER_MEMORY_MANAGED_SIZE` passthrough | New `SignalJobConfig` key → `taskmanager.memory.managed.size` in `applyRuntimeOptions`; unset → deployment authoritative; blank → fail closed. |
| `TASK_MANAGER_NETWORK_MEMORY_MAX` passthrough | New key → `taskmanager.memory.network.max` (+ `.min` pinned below it); p16 embedded deploy previously failed with "required 17, but only 13 available" (64 MB local default). |
| E2E count engine | LOG tables counted as the sum of end offsets via `listOffsets` (a LOG batch scan poisons the shared connection for the next KV scan, measured 2026-08-16); KV tables counted as the max over up to 3 scans (concurrent upsert can expose a partial snapshot), per-sample scans lazy (1 pass unless a dip). |
| E2E process plumbing | faketool + ingestion streams drained on daemon threads (synchronous `transferTo` blocked forever on long-lived servers); both streams drained, not just stderr; 250 ms → 5 s scan poll. |
| Runner script | `E2E_RUN_MINUTES` default 5 → 30; `TASK_MANAGER_MEMORY_MANAGED_SIZE` default 2048m + export; `STATE_BACKEND=rocksdb` default + export (production backend — rule 9); `INGESTION_CLASSPATH` actually computed via `dependency:build-classpath -Dmdep.outputFile` and exported (previously exported unset → empty classpath silently killed the ingestion subprocess). |
| Evidence | Investigation dossier copied to `logs/tracker-14/16-signal-chain-e2e-investigation-20260817.md`. |

## Verification

- `RuntimeOptionsTest` focused run: green (incl. the 3 new passthrough tests).
- Compute module suite: re-run post-port; counts re-measured in
  `01-foundation.md` (C6 line + status banners).
- `SignalChainLiveE2ETest` remains env-gated (`SIGNAL_CHAIN_E2E=true`); the
  live 30-min run against the dev cluster is the next verification step
  (tracker-14, P7-adjacent backlog).
- `docs-audit` C14 change-control check: this record carries all six required
  fields.
