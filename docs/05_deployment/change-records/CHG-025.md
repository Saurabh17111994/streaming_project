# CHG-025 — signal-job remaining-work plan resolution: doc fixes, P7 battery re-run, P10 rehearsal re-run (Design-B), 50k baseline bottleneck record

```text
change_record_id: CHG-025
scope: docs, test-evidence (18-signal-job-remaining-work-plan execution)
owner: Compute and Strategy Teams
date: 2026-08-17
affected_artifacts: docs/08_implementation/04-signal-job.md, docs/08_implementation/09-production-swarm.md, docs/08_implementation/11-testing-and-release.md, docs/08_implementation/18-signal-job-remaining-work-plan.md, logs/tracker-14/p7-battery-design-b-20260817.md (gitignored logs/, per convention), logs/tracker-14/p10-rehearsal-design-b-20260817.md (gitignored), logs/tracker-14/sig-perf-001-50k-baseline-20260817.md (gitignored), code/02_services/02_compute/src/test/java/com/trading/compute/signaljob/SignalJobCheckpointFailureIntegrationTest.java (Item B — tracked in git as d548dd0, no content change)
compatibility_class: COMPATIBLE
savepoint_impact: none — no state layout, topology, or operator change; this plan landed docs + evidence only (Item B tracked an already-written env-gated test). The P10 rehearsal additionally PROVED restore compatibility of the existing Design-B artifact: four strict restores (allowNonRestoredState never set) from archived chk-179 and from the dual-sink era's own chk-198, checkpoint counter continuing 179 → 217 across runs, KV key count frozen at 1,025 in all four
test_updates: NO NEW TESTS — compute suite unchanged (292 run / 0 failures / 17 env-gated skips, module-local mvn -o test). ITEM B LANDED 2026-08-17 — SignalJobCheckpointFailureIntegrationTest (SIG-FAIL-001, written under CHG-024) committed as d548dd0, nothing else in that commit. ITEM C EVIDENCE 2026-08-17 — P7.2/P7.3 battery re-run on Design-B via the 30-min E2E runner + native OTel series in OpenObserve: throughput NOT ACHIEVED (measured feed/tablet write-path ceiling 58.9–59.7k rows/s, topology-independent), latency p99 NOT MEASURABLE (accepted outcome, native-series evidence), checkpoint failures PASS / duration FAIL (replay-inflated state, recorded not inflated), memory N/A to harness topology; register rows PERF-THROUGHPUT-001 / PERF-LATENCY-001 / DEDUP-MEMORY-001 updated in 11-testing-and-release.md. ITEM D EVIDENCE 2026-08-17/18 — P10.1 isolated rehearsal re-run on Design-B, ALL 10 re-scoped boxes PASS: isolated p10 compose project (remapped ports) + live-data byte-copy + archived chk-179 restore entry point (deviations DB1–DB6 recorded); run 1 cutover (RUNNING 13.9 s strict restore, first checkpoint 6 s ≤ 30 s, KV frozen 1,025, LOG +42); runs 2+3 bounded replay twice from the same offsets (identical +42 re-appends retained as evidence, KV key count byte-identical 1,025 both times); run 4 re-cutover from the era's own chk-198 (counter continued 198 → 199, LOG +0, KV frozen); exposure record captured (126 LOG appends, 0 new KV keys, sampled keys); single-LOG-artifact rollback half recorded VACUOUS on Design-B (DB2). ITEM F EVIDENCE 2026-08-17 — 50k baseline BLOCKED at the feed: the bridge's deliberate single-socket production policy (capacity 1,024 instruments / 20,480 t/s) makes the 2,433-instrument / 48.7k baseline structurally unreachable without a multi-connection decision; bottleneck recorded, no config inflation. ITEM E — BLOCKED (awaiting explicit user approval); E.1–E.6 unticked
rollback_behavior: revert the doc annotations (git history); the evidence files live in gitignored logs/ per convention; delete nothing in code (no code changed). Item B's commit d548dd0 reverts independently (env-gated test, no production coupling). No state readability impact — no checkpoints or savepoints depend on this change
plan_tasks: 18-signal-job-remaining-work-plan.md (Items A/B/C/D/F resolved 2026-08-17; Item E BLOCKED awaiting user approval), 04-signal-job.md (Item A stale-spot fixes + §Absorbed corrective tracker open-backlog annotations + SIG-PERF-001 rows), 09-production-swarm.md (P10 status line EXECUTED + §12 execution results + §13 P10.2/P10.3 ready-runbook), 11-testing-and-release.md (P7 register rows)
```

## What landed and why

`18-signal-job-remaining-work-plan.md` (2026-08-17) was the single execution
plan for everything the 04-signal-job audit found partially implemented, not
implemented, or stale. All items are resolved except E, which stays
user-approval-gated:

| Item | Class | Resolution |
| --- | --- | --- |
| A — 3 stale spots in `04-signal-job.md` | pure docs | DONE — dedup config rows annotated SUPERSEDED (Design B), absorbed-E2E paragraph annotated DONE, C6 truth citation corrected |
| B — commit untracked SIG-FAIL-001 test | git only | DONE — `d548dd0`, exactly one file |
| C — P7 battery + gates on Design-B | live-dev, long | DONE — battery executed, all gates verdicted (PASS/FAIL/NOT-MEASURABLE recorded, no config inflation), register rows updated |
| D — P10.1 isolated rehearsal re-run | live-dev | DONE — all 10 re-scoped boxes PASS on Design-B (evidence `logs/tracker-14/p10-rehearsal-design-b-20260817.md`); P10.2/P10.3 delivered as the ready-runbook in `09-production-swarm.md` §13 |
| E — safety consumer live job run | USER-APPROVAL-GATED | BLOCKED (awaiting explicit user approval) — not run |
| F — 50k baseline envelope | live-dev, long | DONE-as-recorded — bottleneck is the feed's deliberate single-socket policy (1,024-instrument capacity), not compute; baseline stays deferred territory |

No production code path changed anywhere in this plan: Item B tracked an
already-written env-gated test, and everything else is documentation and
evidence. The P10 rehearsal ran the REAL SignalJob topology four times against
the isolated p10 environment (host-side in-process MiniCluster, the DB4
submission mechanism) and proved the Design-B artifact's cutover →
bounded-replay idempotency → rollback → re-cutover cycle end to end. Live
money stayed BLOCKED throughout; the Executor stayed HALTED; no credential
left the gitignored `.env`.
