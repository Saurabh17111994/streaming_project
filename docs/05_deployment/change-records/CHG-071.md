# CHG-071 — Task B4: signal→intent flow verified (intent→fill deferred)

```text
change_record_id: CHG-071
scope: signal-job, execution-intent, requirement
owner: Saurabh (solo operator, DEC-044)
date: 2026-08-21
affected_artifacts: code/02_services/02_compute/src/main/java/com/trading/compute/signaljob/ExecutionIntent*.java (pre-existing), logs/nautilus-execution/b4-signal-intent-e2e-20260821.md
compatibility_class: COMPATIBLE
savepoint_impact: none — verification only; no schema, state, savepoint, or checkpoint format touched. Existing deployments with EXECUTION_INTENT_ENABLED=false see zero behavior change.
test_updates: verified existing 18 ExecutionIntent* tests (Builder 5, TableColumnsAgreement 4, ProducerFunction 2, ContextResolver 3, FeedProtocol etc.) — all green; full compute 292/0, executor lib 148/0, Go 18.7s. No new test files — the signal→intent half was already proven.
rollback_behavior: n/a — no code change; EXECUTION_INTENT_ENABLED defaults false (fail-closed), branch absent unless explicitly enabled
plan_tasks: docs/plans/2026-08-25-live-readiness-unified-plan.md (Task B4)
```

## Summary

B4 verification confirms the **signal→intent** half you already have:
`SignalCandidates → ExecutionIntentBuilder (ei-id-v1 / ei-request-v1
ImmutabilityProtocol) → Execution_Intent LOG`, gated by
`EXECUTION_INTENT_ENABLED` (defaults false, fail-closed, branch absent).
18 focused tests green.

The **intent→fill** half (`Execution_Intent LOG → Gateway →
NautilusIntentClient → go-arrow bridge → Fills/Order_Lifecycle/Positions`)
needs the live Fluss cluster + gateway + Nautilus on compose + broker sandbox
(`BI-EQ ×1` paper order) and is deferred to **D-era** on prod VMs (tracked
with AC-paper-order A2→A3→A5). B4.2/B4.3 honest TODO until then.

## Evidence

`logs/nautilus-execution/b4-signal-intent-e2e-20260821.md`
