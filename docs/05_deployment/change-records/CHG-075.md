# CHG-075 — Task A4-error: Arrow REST capability matrix error half (VM-ARROW-010)

```text
change_record_id: CHG-075
scope: execution-bridge, arrow-rest, requirement, version-compatibility
owner: Saurabh (solo operator, DEC-044)
date: 2026-08-21
affected_artifacts: code/02_services/06_execution_bridge/go-bridge/arrow_capability_test.go, logs/nautilus-execution/a4-arrow-rest-error-20260821.md
compatibility_class: COMPATIBLE
savepoint_impact: none — bridge error-path tests only; no Fluss schema, state, savepoint, or checkpoint format touched. Existing live/sandbox modes see zero behavior change.
test_updates: new code/02_services/06_execution_bridge/go-bridge/arrow_capability_test.go — TestArrowRestCapability 4 legs (auth_failure_reauth_then_disabled: 1 reauth→broker_disabled; fifteen_second_timeout_is_unknown_no_retry: UNKNOWN no retry; one_attempt_to_one_order_correlation: coalesce vs request_id_reuse_violation 409; documented_rejection_stays_rejected: 400→REJECTED) — go test -race PASS (1.12s), full suite PASS (0.21s)
rollback_behavior: delete arrow_capability_test.go; Arrow error-half evidence reverts to prior fake_arrow_broker_test.go legs (still PASS but not aggregated as VM-ARROW-010 matrix). No state to migrate.
plan_tasks: docs/plans/2026-08-21-live-readiness-master-plan.md (Task A4 — error half)
```

## Summary

Closes the **error half** of VM-ARROW-010 on this laptop (no market needed):
`401` auth failure → one TOTP re-auth → `broker_disabled` (A1-harness reuse),
15 s `UNKNOWN` timeout with no internal retry, one-attempt-to-one-order
correlation (`RequestID`+fingerprint idempotency vs `request_id_reuse_violation`),
and well-formed `400 → REJECTED`. Four fake-bridge legs on `go test -race`.

The **success half** — one live `BI-EQ ×1` sandbox order through
`Gateway → Nautilus → Arrow REST` returning `broker_order_id` — stays
`TO_BE_VERIFIED` until the market-hours batch (A2/A3). Row 8 of
`docs/08_implementation/12-version-compatibility-evidence.md` is therefore
still gated.

## Evidence

`logs/nautilus-execution/a4-arrow-rest-error-20260821.md`
