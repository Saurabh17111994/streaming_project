# CHG-084 — Task A2.5: T9_ORDER_SANDBOX harness staged (live leg 🔒 on A2.3/A2.4 + T4 wiring)

```text
change_record_id: CHG-084
scope: execution, testing
owner: Saurabh (solo operator, DEC-044)
date: 2026-08-21
affected_artifacts: CHG-084.md, code/01_platform/04_scripts/t9_order_sandbox.py, code/01_platform/04_scripts/tests/test_12_t9_order_sandbox.py, docs/plans/2026-08-21-live-readiness-master-plan.md
compatibility_class: COMPATIBLE
savepoint_impact: none — a new read-only harness + tests; no schema, state, savepoint, checkpoint, or runtime change. The live place->poll->assert->cancel leg is implemented but gated and honestly classified LIVE-CHAIN-UNWIRED until nautilus has bridge forwarding (T4 wall in http.rs) and A2.3/A2.4 sandbox config is live.
test_updates: +11 offline tests (tests/test_12_t9_order_sandbox.py). Trust anchors: (1) gateway-envelope signing port pinned to constants from a REAL JVM run of the production GatewayProtocol.java (jackson 2.16.1, 2026-08-21) — payload hash, canonical, HMAC auth all byte-equal; (2) A2.1 premise asserted against sources (SignalJobConfig EXECUTION_INTENT_ENABLED, DurableIntentDispatcher + NautilusIntentClient); (3) DDL poll columns for the three A2.4 assert tables; (4) T9_APPROVED_BY placement gate fail-closed; (5) live classifier: 202/503 -> LIVE-CHAIN-UNWIRED exit 3, 401 -> FAIL exit 1, no flag -> BLOCKED exit 2. pytest 11/11; full suite 351 PASS.
rollback_behavior: n/a — new standalone harness + tests; deleting both returns to the A2.5-unstarted state. No state to migrate.
plan_tasks: docs/plans/2026-08-21-live-readiness-master-plan.md (Task A2)
```

## Summary

Adds the **`T9_ORDER_SANDBOX` Python integration** A2.5 asks for, in the t8
harness convention, with the entry contract encoded exactly:

* **Reuses `t8_sandbox_contract_check.py`** — running it 12/12 is the harness's
  first offline check.
* **Offline leg (runs today, 20 checks):** `execution-t3` compose shape
  (internal `execution-net`, zero host ports, bridge defaults `disabled`,
  nautilus `EXECUTION_ENABLED=false`), the A2.1 premise, the three DDL tables +
  poll columns, and the signing parity vs the JVM.
* **Signing port:** byte-exact Python port of `GatewayProtocol` (writer:
  `06_execution_gateway` Java; verifier: `04_executor` Rust — both agree:
  lowercase hex, 10-line canonical, HMAC-SHA256). The fixed vector constants
  (`JW_*`) were produced by compiling and running the real `GatewayProtocol.java`
  on this host, so a transcription slip would fail the parity tests here instead
  of as a 401 in front of the live verifier.
* **Live leg (`--live`, in-network via docker exec/run — the profile publishes
  no host ports):** place the BI-EQ x1 envelope (t9paper INPUT-11 safe
  instrument) -> poll Execution_Intent / Order_Lifecycle / Execution_Attempts
  -> cancel. Honest classification when the stack is reachable but the chain is
  not yet wired: **exit 3 LIVE-CHAIN-UNWIRED** (nautilus `http.rs` acknowledges
  a valid envelope "without executing — no LiveNode wiring yet"; the gate is
  `HALTED` until approval). Never reported as a fake PASS.
* **Placement gate:** no order may be placed without `T9_APPROVED_BY=saurabh`
  (fail closed, exit 2 BLOCKED) — the A2 DoD "no real order possible without
  the flag".

## What remains (honest)

* `A2.1`-`A2.4` live steps still open (gate enable + sandbox broker config +
  the T4 bridge-forwarding wiring + market hours) before the harness's live leg
  can reach exit 0. The harness self-check proves the *classifier* today; the
  full round-trip assertion runs at `A2.6`.
