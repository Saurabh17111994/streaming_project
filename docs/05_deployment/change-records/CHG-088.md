# CHG-088 — Monday-gate step-8 E2E regression fix + python truth-derivation hardening

```text
change_record_id: CHG-088
scope: ingestion, platform scripts, gate
owner: Saurabh (solo operator, DEC-044)
date: 2026-08-24
affected_artifacts: CHG-088.md, code/02_services/01_ingestion/go-bridge/main.go, code/02_services/01_ingestion/src/test/java/com/trading/ingestion/FullStackE2ETest.java, code/01_platform/04_scripts/run-monday-gates.sh, code/01_platform/04_scripts/tests/test_stale_table_kind_scan.py, code/01_platform/04_scripts/tests/test_execution_network_check.py, docs/08_implementation/01-foundation.md (test counts unchanged — no tests added/removed)
compatibility_class: COMPATIBLE
savepoint_impact: none — test-only fake-broker auth mode in the Go bridge (flag-gated, default absent), one test env var, one gate redirect, two python-test fixtures/test. No production runtime path changes; production auth remains fail-closed trio → AutoLogin.
test_updates: no test count changes (truth triple stays 464/247/387; suite triples 464/0/1, 247/0/8, 387/0/17). FullStackE2ETest now sets ARROW_FAKE_BROKER=1; test_stale_table_kind_scan silent-path fixtures derive from SUITE_TRIPLE_TRUTH/C6_TRIPLE_TRUTH constants; test_execution_network_check injected key ARROW_TOKEN → ARROW_APP_SECRET.
rollback_behavior: revert the five files to pre-CHG-088 state; the fake-broker branch is dead code without the env flag, the gate redirect changes only log capture (stderr previously lost), and the scanner fix is advisory-only (literal stale numbers were the bug, not the constants).
plan_tasks: docs/plans/2026-08-21-live-readiness-master-plan.md A6.1 (gate green re-proof) — gate step 8 was the only failing step; steps 1–7 already PASS.
```

## Summary

`make gate` (run-monday-gates.sh) step 8 (`bg_4` ingestion-int suite) failed on
`FullStackE2ETest.fullStackFakeBrokerToFluss` with a core regression caused by
the TOTP migration (commit `70058b0`): the Go bridge now AutoLogins against the
real Arrow API in every mode, but the E2E harness passes fake `e2e-user`
credentials → real API `"user not found"` → `BRIDGE_CRASH exitCode=2` ×2 → the
test's `event=disconnect` marker never arrives, ticks=0.

Fix (test-only, production unchanged):

* `go-bridge/main.go`: `ARROW_FAKE_BROKER=1` env mode — skips AutoLogin,
  `client.SetToken("fake-token")`, no-op `refreshAuth`; production path
  (flag absent) untouched: fail-closed trio → AutoLogin → reconnect.
* `FullStackE2ETest.java`: sets `ARROW_FAKE_BROKER=1` in the broker env.
* `run-monday-gates.sh:203`: step-8 `mvn` stdout/stderr now redirected to
  `$JAVA_LOG` — previously unredirected, so `java-suite.log` was never created
  and gate diagnosis lost the surefire output entirely.
* `test_stale_table_kind_scan.py`: silent-path fixtures derive from
  `s.SUITE_TRIPLE_TRUTH`/`s.C6_TRIPLE_TRUTH` instead of re-typed literals
  (third occurrence of truth-bump breakage would otherwise recur).
* `test_execution_network_check.py`: injected key `ARROW_TOKEN` →
  `ARROW_APP_SECRET` (ARROW_TOKEN no longer exists in `ORDER_ARROW_ENV`).

## Decisions

- **Fake-broker mode in the bridge, not the vendored SDK**: SDK tree is
  pinned/off-limits; the flag is test-only and absent in all production
  launches, so fail-closed production auth semantics are preserved exactly.
- **Gate does not re-verify what the user reported**: after the fix, the
  isolated E2E passed (`Tests run: 1, Failures: 0, Errors: 0`, 15.3 s,
  `finalTicks=25 errors=0 restarts=0`, env
  `INGESTION_INT_TEST_E2E=true`) and the full `bg_4` four-flag suite then
  passed 247/247 (0 F, 0 E, 1 known env-gated skip).
- **Evidence**: `logs/soak/monday-gates-20260824-124159/` (python fail, 12:41)
  and `logs/soak/monday-gates-20260824-145119/` (step-8 fail, 14:51 —
  java-suite.log absent due to the redirect bug, now fixed).

## Verification

- `python3 -m unittest discover -s code/01_platform/04_scripts/tests` → 276 OK.
- `make stale-tables` → exit 0.
- `INGESTION_INT_TEST_E2E=true mvn -o test -pl 02_services/01_ingestion -am -Dtest=FullStackE2ETest -Dsurefire.failIfNoSpecifiedTests=false` → PASS.
- `INGESTION_INT_TEST_E2E=true INGESTION_INT_TEST_FLUSS=true INGESTION_INT_TEST_MANIFEST=true INGESTION_INT_TEST_PERF=true mvn -o test -pl 02_services/01_ingestion -am` → 247/247, 0 F, 0 E, 1 skipped.
- `go build ./... && go vet ./...` (bridge) → clean.
