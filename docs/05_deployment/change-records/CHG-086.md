# CHG-086 — Task D7 prep: O2 dashboards seeded locally (idempotent; thresholds/OPS-FAIL-001 remain 🔒)

```text
change_record_id: CHG-086
scope: observability, deployment
owner: Saurabh (solo operator, DEC-044)
date: 2026-08-21
affected_artifacts: CHG-086.md, Makefile, docs/plans/2026-08-21-live-readiness-master-plan.md
compatibility_class: COMPATIBLE
savepoint_impact: none — dashboard provisioning only; no schema, state, savepoint, or checkpoint change. The live local OpenObserve org `default` already carried the 4 corpus dashboards; the seed was idempotent (created=0, untouched=4).
test_updates: none new — existing tests/test_seed_dashboards.py 5/5 PASS re-verified (manifest evidence fields, v8 corpus structure, panel/query contracts, no secrets, credential gate). Full suite + Makefile help/PHONY targets verified.
rollback_behavior: n/a — the seed tool is idempotent; removing the new `seed-dashboards` Makefile target returns to pre-state. No data mutation occurred (untouched=4).
plan_tasks: docs/plans/2026-08-21-live-readiness-master-plan.md (Task D7)
```

## Summary

D7 prep: the OpenObserve dashboard corpus
(`code/01_platform/01_docker/openobserve/dashboards/` — Safe to Trade, Orders and
Fills, Market Data Ingestion, Storage and EOD) is verified live in the local O2
instance and a repeatable `make seed-dashboards` target is now wired:

* **Idempotent run, real instance**: `make seed-dashboards` -> rc 0,
  `RESULT: created=0 updated=0 untouched=4` (corpus already live; nothing
  mutated).
* **No-drift proof (read-only)**: stored v8 tabs are semantically identical to
  the corpus for all 4 dashboards — same panels/titles/layouts/queryType/SQL;
  the only deltas are O2 save-time housekeeping keys (`vrlFunctionQuery`,
  `base_map`, `map_view` nulls). Because `--force` compares raw `tabs`, a force
  would oscillate against O2's owned keys forever — deliberately NOT run.
* **Credential hygiene preserved**: the tool still refuses to run with a blank
  `O2_PASSWORD` (exit 2); creds come from `code/01_platform/01_docker/.env`
  (gitignored), never printed.

## Remaining in D7.1/D7.2 (honest)

* Alert thresholds **from measured D4/D5 data** — 🔒 external measured data not
  yet available; corpus/static defaults stand until then.
* **`OPS-FAIL-001`** O2-outage-durable-audit drill — a live D-era stack drill
  (durable half already covered by E5b R2 audit store + local logs evidence).
* D7.2 final evidence + CHG at D7 completion — note: D7.2's plan text names
  `CHG-078`, but that number was already allocated to E4 docs-reconciliation;
  the final D7 record will take its own next-free CHG.
