# CHG-031 — forming-bar emission-profile documentation: 2 profile unit tests, C6 truth 292→294, CHG-023 artifact-existence fix

```text
change_record_id: CHG-031
scope: test additions + documentation + C6 truth bump — no production runtime behavior changed (FormingBarDetectionFunction logic untouched; javadoc only)
owner: Compute (Signal Job) Team
date: 2026-08-18
affected_artifacts: code/02_services/02_compute/src/test/java/com/trading/compute/signaljob/FormingBarDetectionFunctionTest.java (+2 emission-profile tests — driftingFeedFiresOncePerWarmWindow, qualifyingTickBeforeWarmUpIsLostForThatWindow), code/02_services/02_compute/src/main/java/com/trading/compute/signaljob/FormingBarDetectionFunction.java (javadoc — corrected the "deterministic integration testing" overclaim; documented the warm-up-race emission profile), docs/08_implementation/04-signal-job.md (emission-profile note + C6 truth citation + Item-E line-800 count annotation), docs/08_implementation/01-foundation.md (C6 truth 341/234/292 → 341/234/294), docs/08_implementation/00-start-here.md, docs/08_implementation/02-schema-storage.md, docs/08_implementation/03-ingestion.md, docs/08_implementation/11-testing-and-release.md, docs/08_implementation/17-open-items-plan.md, docs/08_implementation/18-signal-job-remaining-work-plan.md (C6 truth citations + suite-count claims 292 → 294), code/01_platform/04_scripts/stale_table_kind_scan.py (TEST_COUNT_TRUTH compute 292 → 294 + comment), Makefile (C6 truth help text), docs/05_deployment/change-records/CHG-023.md (affected_artifacts parenthetical — dropped the `.java` suffixes from the DELETED StallGuardedSink mention so C14 resolves against the live tree)
compatibility_class: COMPATIBLE
savepoint_impact: none — no topology, operator, or state change; test/docs/config-meta only
test_updates: +2 FormingBarDetectionFunctionTest legs (9 → 11 in the class; compute suite 292 → 294 run / 0 failures / 17 env-gated skips, plain-suite re-verified). The +2 pin the placeholder's deterministic invariants: (1) a drifting feed fires exactly once per warm window with fire-once holding, and (2) a window whose qualifying tick lost the warm-up race is skipped permanently. docs-audit (C6 341/234/294 + C14 all records), stale-table scan (0 unannotated), and test-audit-r2 (119 OK) all green after the change.
rollback_behavior: revert the test additions and the javadoc/doc text — no production code or config gates change; the C6 truth bump must be reverted together with the +2 tests (the counts are machine-validated).
plan_tasks: 04-signal-job.md (forming-bar emission-profile documentation — CHG-030 follow-up: why the placeholder fires ~14–18 of 18 warm windows per token on the flat P6 feed, the warm-up race, and the integration-test scoping rule)
```

## What happened

Following CHG-030 (which scoped the integration tests to the window-driven rule), the
forming-bar placeholder's own emission profile was investigated: the deterministic flat P6
feed produced ~17 forming-bar candidates per token (≈34 LOG rows) in the replay test and 14
per token in the RocksDB test — despite the fire-once-per-window semantics. This record
documents the root cause and pins the deterministic invariants in unit tests.

## Root cause — the drift feed IS a breakout to the mirrored rule

- Every P6 window `w` carries a **+50 paise high tick** (prices `10000+w`, `10050+w`,
  `9950+w`, `10000+w`) and prices drift **+1/window**.
- The placeholder compares the live forming close against the max HIGH of the
  `FORMING_LOOKBACK_CANDLES` (default 5) strictly-prior completed candles —
  `maxHigh ≈ 10050+(w−1)`.
- At the +50 tick, the forming close `10050+w > maxHigh` → breakout ✓, bullish ✓,
  trend ✓ → **every warm window qualifies once** (w5..w22 = 18 per token).
- **Fire-once per (instrument, window) IS honored** — each fire is a distinct window; there
  are no duplicate-window fires.
- **Which windows actually fire is scheduling-dependent:** the lookback ring fills from the
  completed-candle stream racing the tick stream at the `connect`; a window's +50 tick is its
  only qualifying instant, so if the ring was not yet warm at that instant the window is
  skipped permanently. w5 usually loses (the ring fills exactly then); heavier scheduling
  (RocksDB + checkpoints) loses more → the observed 17 vs 14 per token, and the stall test's
  non-deterministic 3–5.

## The fix — document (deliberately not a detector redesign)

The detector is an explicit placeholder (the real strategy replaces the rule without pipeline
changes; candidates are observational — no consumer). A deterministic redesign (evaluate only
at candle close) would contradict the handoff's core requirement (REQ-FC-007: Business Logic
sees the live forming state before the candle closes). So:

1. **Javadoc corrected** in `FormingBarDetectionFunction` — the "exists for deterministic
   integration testing" claim was an overclaim: fire-once is deterministic, WHICH windows fire
   is not (warm-up race). The profile is now documented at the source.
2. **+2 unit tests** pin the deterministic invariants through the operator harness:
   `driftingFeedFiresOncePerWarmWindow` (every warm window on a drifting feed fires exactly
   once; fire-once holds across updates and windows) and
   `qualifyingTickBeforeWarmUpIsLostForThatWindow` (a window whose qualifying tick arrived
   before the ring was full never fires, even after the ring completes).
3. **Dossier note** in `04-signal-job.md` §Forming-bar and candidate interface — the emission
   profile, the ~14–18-of-18 range, and the rule that integration tests must scope to the
   window-driven rule (CHG-030) rather than pin forming-bar counts.

## Collateral fixes included in this commit

- **C6 truth 341/234/292 → 341/234/294** (the +2 tests) across `01-foundation.md` L42 (the
  C6 source line), the four citation dossiers, the `04-signal-job.md` truth mention, the
  scanner's `TEST_COUNT_TRUTH`, the Makefile help text, and the suite-count claims in
  `17-open-items-plan.md` / `18-signal-job-remaining-work-plan.md`.
- **CHG-023 artifact-existence fix:** the record's `affected_artifacts` parenthetical retained
  path-shaped `StallGuardedSink.java`/`StallGuardedSinkTest.java` tokens of the files CHG-023
  itself deleted; C14's repo-wide basename search could previously resolve them only via stale
  pre-CHG-023 worktree checkouts (pruned 2026-08-18), so the check was environmentally
  dependent. The `.java` suffixes are now omitted (CHG-022's convention); C14 passes
  standalone.
- **Worktree-staleness recovery:** two doc copies (`04-signal-job.md`,
  `18-signal-job-remaining-work-plan.md`) initially reverted CHG-026 (`eab084e`) content
  because the session worktree was 6 commits behind main; both were restored to HEAD and the
  intended edits re-applied. Final diffs contain only the intended lines.

## Verification

- `FormingBarDetectionFunctionTest`: **11/11 pass** (was 9).
- Plain compute suite: **294 run / 0 failures / 17 env-gated skips**, BUILD SUCCESS.
- docs-audit: **all checks pass** — C6 (341/234/294 matches surefire), C14 (all 29 records,
  incl. the CHG-023 fix validating standalone).
- stale-table scan: **0 UNANNOTATED**, exit 0.
- test-audit-r2: **119 OK**.

## Status

- **Done.** The placeholder's emission profile is documented at the source and in the dossier,
  the deterministic invariants are pinned by unit tests, and the C6 truth follows the +2 tests.
- No production runtime behavior changed. The real strategy (Slice 2.2 Q2) replaces the rule
  without pipeline changes.
