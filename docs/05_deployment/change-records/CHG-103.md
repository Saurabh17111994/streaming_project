# CHG-103 — VM-PYTHON-002 pin fix + image hardening + SEC-IMAGE-001 policy + ddl-smoke count fix

2026-08-25 follow-on to CHG-102: closes the VM-PYTHON-002 pin drift, re-scans the
container images (VM-IMAGES-012), pins the SEC-IMAGE-001 vulnerability policy, and
fixes a stale table count in the DDL apply smoke. No runtime/topology/DDL change.

```text
change_record_id: CHG-103
scope: decision, gate-behavior, topology-secret (image pinning)
owner: saurabh
date: 2026-08-25
affected_artifacts: code/01_platform/01_docker/ddl-apply/Dockerfile (runtime base eclipse-temurin:17-jre -> python:3.11-slim + openjdk-21-jre-headless; deadsnakes PPA rejected — GPG fetch flaky in builds); code/01_platform/01_docker/ddl-apply/ddl-apply-run.sh (new `version` subcommand asserting python series vs PYTHON_VERSION pin); code/01_platform/04_scripts/versions.pin (PYTHON_VERSION=3.11.9 added — was never pinned); code/01_platform/04_scripts/version_matrix.yaml (VM-PYTHON-002 COMPATIBLE_WITH_LIMITATION->COMPATIBLE, DRIFT_RECORDED_PIN_UNSATISFIED->EVIDENCE_RECORDED; VM-IMAGES-012 re-scan 2026-08-25); code/01_platform/04_scripts/ddl_apply_smoke.py (TABLES=26 -> TABLES=27, 3 expectations); docs/08_implementation/11-testing-and-release.md (SEC-IMAGE-001 vuln policy pinned); docs/08_implementation/12-version-compatibility-evidence.md (VM-PYTHON-002 row + VM-IMAGES-012 row + drift commentary + DDL-gate note); docs/08_implementation/01-foundation.md (Python runtime status COMPATIBLE); logs/version-matrix/images-012-20260825T141500Z.md (fresh scan + waivers, gitignored); logs/version-matrix/openobserve-20260825.spdx.json (SBOM gap closed, gitignored)
compatibility_class: COMPATIBLE
savepoint_impact: none — image/runtime packaging + docs only; no state, checkpoint, or DDL change
test_updates: ddl_apply_smoke.py scenarios 1/2/3 pinned TABLES=27 (was 26); new ddl-apply-run.sh `version` subcommand; full Monday gate re-run 2026-08-25 13/13 PASS (evidence logs/soak/monday-gates-20260825-203040/)
rollback_behavior: revert Dockerfile base to eclipse-temurin:17-jre (returns to 3.14.4 drift) or pin python:3.11.9-slim (stale OS, 73/11 vulns); revert versions.pin PYTHON_VERSION; images rebuilt with `docker compose build --no-cache`
plan_tasks: 12-version-compatibility-evidence.md, 11-testing-and-release.md, docs/plans/2026-08-25-live-readiness-unified-plan.md (U0 hygiene, U4.4 evidence gate)
```

## Change

1. **VM-PYTHON-002 pin drift resolved.** The ddl-apply runtime stage apt-get-installed
   the distro python on `eclipse-temurin:17-jre` (Ubuntu 26.04) — drifted to 3.14.4, and
   Python was never actually in `versions.pin` (so nothing enforced the 3.11.9 pin).
   Fix: runtime stage builds on `python:3.11-slim` (3.11.16, Debian trixie — the official
   image *is* the pin series) + `openjdk-21-jre-headless` (trixie has no JRE 17; the
   precompiled Java 17 engine runs on 21 — `validate` + `self-test` PASS). Added
   `PYTHON_VERSION=3.11.9` to `versions.pin` and a `ddl-apply version` subcommand that
   asserts the running python series matches the pin (fails loudly on future drift).
   (Deadsnakes PPA tried first — `GPGKeyTemporarilyNotFoundError` in Docker builds; the
   python-base switch is strictly more robust.)

2. **VM-IMAGES-012 re-scan + SBOM closure.** Re-scanned all 13 runtime images with the
   cached trivy DB (2026-08-25). The base switch cut the ddl-apply image vulns
   81/12 -> 14/3 (trixie, all `fixed=None` = latest patch); webhook-receiver
   `python:3.11-slim` 50/3 -> 14/3. Captured the missing OpenObserve SBOM via trivy SPDX
   (`openobserve-20260825.spdx.json`). Library-layer (Java) scan stays `scan-unavailable`
   (Java DB mirror network-blocked — same as 2026-08-24).

3. **SEC-IMAGE-001 vulnerability policy pinned** (was TO_BE_VERIFIED): CRITICAL-with-fix
   **blocks** deployment; CRITICAL-no-fix and HIGH-with-fix require a dated, owner-signed
   waiver in the scan evidence; HIGH-no-fix is accepted risk, documented each scan.
   Dated waivers recorded for the fixable findings (golang, zookeeper, cadvisor, fluss,
   openobserve, minio) — none blocks promotion under the policy.

4. **ddl_apply_smoke.py stale count fixed.** The smoke pinned `TABLES=26` in scenarios
   1/2/3 but the live apply reports 27 (the 27th table landed since the pin was written).
   Updated to `TABLES=27`; live re-run against `fluss-coordinator:9123` — scenarios 1-4
   all PASS.

## Evidence

- `logs/soak/monday-gates-20260825-203040/` — full Monday gate 13/13 PASS (static,
  compose config, python 291, entrypoint, Go -race, E2E binaries, image staleness,
  Java full gate, full doc audit, DDL smoke (env-skip), Schema/Perf, SIGTERM-drain).
- `logs/version-matrix/images-012-20260825T141500Z.md` — fresh scan + waivers.
- `logs/version-matrix/openobserve-20260825.spdx.json` — OpenObserve SBOM.
- `docker compose run --rm ddl-apply version` — Python 3.11.16 matches pin series.
- `docker compose run --rm ddl-apply self-test` — DDL-APPLY-RESULT PASS exit=0.
- `FLUSS_BOOTSTRAP=localhost:9123 python3 ddl_apply_smoke.py` — 4/4 scenarios PASS.
