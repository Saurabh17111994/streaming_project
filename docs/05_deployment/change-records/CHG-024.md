# CHG-024 — open-items-plan resolution: SIG-FAIL-001 test, R2 checkpoint storage, 30-min soak evidence, telemetry closures

```text
change_record_id: CHG-024
scope: decision, test-evidence (open-items-plan execution)
owner: Compute and Strategy Teams
date: 2026-08-17
affected_artifacts: docs/08_implementation/17-open-items-plan.md, docs/08_implementation/04-signal-job.md, docs/08_implementation/11-testing-and-release.md, docs/08_implementation/00-start-here.md, docs/08_implementation/01-foundation.md, docs/08_implementation/02-schema-storage.md, docs/08_implementation/03-ingestion.md, code/02_services/02_compute/src/test/java/com/trading/compute/signaljob/SignalJobCheckpointFailureIntegrationTest.java, code/01_platform/01_docker/.env (gitignored — R2 checkpoint URI + credentials, never committed)
compatibility_class: COMPATIBLE
savepoint_impact: none — no state layout, topology, or operator change; Item 1 adds an env-gated test only, Item 2 wires a production-only config value (dev stays on file://), Items 3–5 are evidence and annotations
test_updates: ITEM 1 LANDED 2026-08-17 — +1 SIG-FAIL-001 SignalJobCheckpointFailureIntegrationTest (env-gated COMPUTE_INT_TEST_SIG_FAIL=true, host-runnable MiniCluster + file:// checkpoints: healthy control checkpoint, then the job-id checkpoint dir stripped of write → next checkpoint fails → the production FIXED_DELAY restart route drives exactly 3 RESTARTING episodes then FAILED, cause contains "checkpoint"; compute suite 291 → 292 run / 0 failures / 17 env-gated skips). ITEM 3 EVIDENCE 2026-08-17 — 30-min soak PASSED (SignalChainLiveE2ETest, 1822 s, BUILD SUCCESS): feature +123 904 rows, 117 samples all RUNNING, 179 consecutive EXACTLY_ONCE checkpoints, 0 restarts, 0 checkpoint-1 stalls; evidence logs/tracker-14/e2e-30min-soak-20260817.md (gitignored logs/, per convention). ITEM 4 CLOSED-AS-COVERED 2026-08-17 — no code; the dossier's "Pending (Slice 2+)" telemetry row annotated with the native series verified live in OpenObserve during the soak (flink_jobmanager_job_numrestarts 178 rows all 0.0, numberoffailedcheckpoints all 0.0, numberofcompletedcheckpoints rising to 178, lastcheckpointrestoretimestamp constant at job start; sink keep-up via numrecordsoutpersecond_rate — the bare meter-count stream exports empty, the _rate substream carries the data). ITEM 5 CLOSED-AS-DEFERRED 2026-08-17 — no code; CANDLE-KV-REPLAY-001 deferral accepted as final disposition (candle table KV-only since 2026-08-13 — the LOG-vs-KV surface is gone; signal LOG/KV convergence covered by existing native telemetry; dedicated counters would contradict the CHG-023 native-first direction). ITEM 6 LANDED 2026-08-17 (by the prior agent) — doc-drift fixes in 04-signal-job.md (gate name COMPUTE_INT_TEST_SIGNAL_KV corrected, deleted SigState002/003 classes annotated, skip count reconciled). ITEM 2 LANDED 2026-08-17 (by the prior agent) — production checkpoint storage decided as Cloudflare R2: CHECKPOINT_DIR=s3://tradingticks-aug-2026/flink-checkpoints wired in the gitignored .env; SignalJobConfig fail-closed validation accepts it; SignalJobObjectStoreCheckpointIntegrationTest PASSED live against the real bucket (run-1786975842757, RocksDB chk-2 artifacts on the object store, strict restore resumed 900 = 600 + 300)
rollback_behavior: revert the dossier/plan annotations (git history); delete SignalJobCheckpointFailureIntegrationTest.java (env-gated, no production coupling); revert the .env CHECKPOINT_DIR to the file:// dev override (production gate only enforces the S3 URI when DEPLOYMENT_ENV=production). No state readability impact — no checkpoints or savepoints depend on this change
plan_tasks: 17-open-items-plan.md (all 6 items resolved 2026-08-17), 04-signal-job.md (§Required telemetry CLOSED-AS-COVERED + DEFERRAL ACCEPTED annotations, §Required tests SIG-FAIL-001 DONE row, SIGNAL-CHAIN-E2E-001 30-min acceptance cell)
```

## What landed and why

`17-open-items-plan.md` (2026-08-17) was the single execution plan for every
item the 04-signal-job audit found still open, partially implemented, or
unverified — feature-path-only scope (`raw_table_1` → validation → dedup →
candles → `feature_candles_15s`). All six items are now resolved:

| Item | Class | Resolution |
| --- | --- | --- |
| 6 — Doc-drift fixes | pure-JVM now | DONE — gate name, deleted-class, and skip-count drift fixed in `04-signal-job.md` |
| 1 — `SIG-FAIL-001` checkpoint-failure test | pure-JVM now | DONE — `SignalJobCheckpointFailureIntegrationTest` proves the job fails closed under the pinned FIXED_DELAY restart route (3 × restart then FAILED, cause ~ checkpoint); control leg clean |
| 2 — Production checkpoint storage | decision-gated | DONE — user chose Cloudflare R2; URI wired in the gitignored `.env`; live P4.2 proof PASSED against the real bucket |
| 3 — 30-min E2E soak | live-dev now | DONE — full 30-min soak PASSED on the final native profile (179 checkpoints, 0 restarts, 0 stalls, feature +123 904 rows) |
| 4 — Sink-latency / restore-count / state-compat telemetry | close-as-covered | CLOSED — native Flink metrics verified live in OpenObserve; no custom counters (native-first per CHG-023) |
| 5 — CANDLE-KV-REPLAY-001 observability | close-as-deferred | CLOSED — deferral accepted as final disposition (KV-only candle table removed the surface; escape hatch stays open) |

No production code path changed: Item 1 is an env-gated test, Item 2 is a
production-only config value behind the `DEPLOYMENT_ENV=production` gate, and
Items 3–5 are evidence and dossier annotations. The out-of-scope boundary
(p99 decision latency, real detection strategy, Trade_Decisions) is unchanged.
