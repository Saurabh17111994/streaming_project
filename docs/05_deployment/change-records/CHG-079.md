# CHG-079 — WP-2 remainder: wire HttpBridgeClient into the production path

```text
change_record_id: CHG-079
scope: executor (Rust), execution-bridge wiring, config, compose, docs
owner: Saurabh (solo operator, DEC-044)
date: 2026-08-21
affected_artifacts: code/02_services/04_executor/src/{config,engine,main,bootstrap}.rs, code/02_services/04_executor/tests/live_go_bridge.rs (exercised), code/01_platform/01_docker/docker-compose.yml, docs/08_implementation/20-close-execution-service-gaps-plan.md, logs/nautilus-execution/wp2-httpbridge-production-path-20260821.md
compatibility_class: COMPATIBLE
savepoint_impact: none — Rust-only wiring + compose env; no schema, state, savepoint, or checkpoint format change. Bridge transport selection is explicit and env-gated; gate still boots HALTED.
test_updates: +5 executor tests (config BRIDGE_AUTH_TOKEN read + bridge_selection fake/http from config + factory http-create offline + runtime http-build halt). cargo test --offline --lib 153 PASS (was 148). Live interop: HttpBridgeClient round-trips the real Go fake bridge (cargo test --offline --test live_go_bridge, 1 PASS). Pre-existing durable.rs clippy lints unchanged/unrelated.
rollback_behavior: revert the 5 source/compose files; service returns to always-FakeBridge (compose still sets BRIDGE_ENDPOINT — with selection absent it was ignored). No state migration.
plan_tasks: docs/plans/2026-08-25-live-readiness-unified-plan.md (new task B9 — HTTP bridge transport into production path)
```

## Summary

Closes the WP-2 remainder (real code item #1 from the 2026-08-21 audit): the production
`HttpBridgeClient` transport (HTTP `/v1/commands` + WS `/v1/events` intake with reconnect) is
now selectable in the executor's live construction path via `BridgeSelection`, env-gated on
`BRIDGE_ENDPOINT` (with `BRIDGE_AUTH_TOKEN`). No endpoint keeps the offline `FakeBridge`
default. The service still boots `HALTED`; this is the wire, not the order (live `BI-EQ ×1`
evidence stays on the market-session batch A2→A3→A5 + DEC-044 sign-off).

## Evidence

`logs/nautilus-execution/wp2-httpbridge-production-path-20260821.md`
(cargo 153 PASS + live Go-bridge interop 1 PASS + bridge healthz `{"mode":"fake","status":"UP"}`).
