# CHG-081 — Task C5.1: scale-path decision record (draft; human choice C5.2 open)

```text
change_record_id: CHG-081
scope: requirement, topology-secret
owner: Saurabh (solo operator, DEC-044)
date: 2026-08-21
affected_artifacts: CHG-081.md, docs/plans/2026-08-21-live-readiness-master-plan.md
compatibility_class: COMPATIBLE
savepoint_impact: none — decision/planning record; multi-connection transport is proven at unit level (C1–C4), actual connection-count activation env is NOT changed by this record and nothing is auto-enabled. No state/savepoint/checkpoint impact.
test_updates: none — record of built-and-verified capability; cites C1–C4 evidence (CHG-066/067/068/069). No new tests in this record.
rollback_behavior: n/a — decision record; if the human later chooses premium-tier, the multi-connection path remains in the tree behind env (ARROW_HFT_CONNECTIONS) and can be left at default 1. No code/state rollback needed.
plan_tasks: docs/plans/2026-08-21-live-readiness-master-plan.md (Task C5)
```

## Purpose

Records the Phase C scale decision exactly as the plan scopes it (Task C5): the
multi-connection bridge path is **built and verified** (C1–C4), the premium-broker-tier
alternative is **documented as the fallback**, and **activation of the full 2,433-instrument
envelope is left to the human** — nothing is auto-enabled by this record.

## 1. What is already built (C1–C4, all DONE)

| Task | Capability | Evidence / CHG |
|---|---|---|
| `C1` bridge fan-out plug-and-play (1→3) | token-shard determinism (2,433→3 slots), per-socket reconnect isolation, aggregate caps (N=1→1,024, N=2→2,048, N=3→2,433/3,072) | `logs/nautilus-execution/c1-multiconn-20260821.md` + CHG-066 |
| `C2` config parity + pin update | `ARROW_HFT_CONNECTIONS` tunable 1..3 on both Java↔Go sides, out-of-range rejected | `logs/nautilus-execution/c2-config-parity-20260821.md` + CHG-067 |
| `C3` losslessness re-validation at scale | per-token 0 lost/0 vanished/0 extra at 1,024/2,048/2,433/3,072 slots; restart re-sharding deterministic | `logs/nautilus-execution/c3-losslessness-multiconn-20260821.md` + CHG-068 |
| `C4` SIG-PERF-001 50k baseline unblocked | 2,433×20 Hz ≈ 48,660 tps ≥ 47,500 floor, per-token exact, N=1 vs N=3 no degradation | `logs/nautilus-execution/c4-sig-perf-50k-20260821.md` + CHG-069 |

## 2. The scale decision (plan default, recorded here)

**Decision recorded:** Phase C builds multi-connection bridge support as the path to the
50k ticks/s envelope **under the current subscription** — no premium-tier purchase is
assumed. The **premium-broker-tier alternative is documented as a manual fallback** (plan
"Scale decision (defaulted)", 2026-08-21) and remains available if the human ever chooses
it.

**Activation of the full 2,433-instrument envelope is left to the human.** The activation
environment (`ARROW_HFT_CONNECTIONS` ≥ 2) is set only at the human's discretion; this
record and the C1–C4 code **auto-enable nothing**. Defaults stay `N=1` / 1,024-instrument
single-socket.

## 3. 🔒 Human choice slot (C5.2)

The human (Saurabh) must record one of:

```text
Date (UTC):  ______________
Decision:
  [ ] (a) Run multi-connection at N×1,024 within the current subscription
        → activation env ARROW_HFT_CONNECTIONS=2..3 set at deploy time (human-triggered CHG).
  [ ] (b) Purchase the premium broker tier (fallback path documented in the plan)
        → subsequent CHG re-records this decision + capability matrix.
  [ ] Defer the decision (multi-connection stays built-but-dormant at default N=1).
Signature:  ______________
```

**DoD:** decision recorded; envelope activation (2,433) gated on the human choice — never
auto-enabled.
