# CHG-011 — ING-E2E-001 harness hardening (LOG_DIR + marker polling) and dev-cluster manifest-24 schema state

```text
change_record_id: CHG-011
scope: gate-behavior
owner: Ingestion Team
date: 2026-08-15
affected_artifacts: code/02_services/01_ingestion/src/test/java/com/trading/ingestion/FullStackE2ETest.java, 11-testing-and-release.md (ING-E2E-001 catalog row)
compatibility_class: COMPATIBLE
savepoint_impact: none — test-harness-only change; no main-code, wire, state, or DDL change
test_updates: no count change (ingestion stays 207/0/8-skips; ING-E2E-001 remains env-gated on INGESTION_INT_TEST_E2E=true). FullStackE2ETest hardened 2026-08-15: (1) the subprocess env now sets LOG_DIR to a writable JUnit temp dir — log4j2's JSON_FILE appender defaults to /data/ingestion/logs and, when that path cannot be created, the root logger drops every event, which had left the assertions blind to the service's startup logs; (2) the fixed 8 s sleep was replaced by polling (250 ms interval, bounded deadlines): 'Fluss connected' and 'arrow-bridge started' within 90 s, then 'event=disconnect' (the faketool --disconnect-after 1 forced close → DROP discontinuity evidence path) within 30 s. Stderr is accumulated in a thread-safe StringBuffer instead of a racy StringBuilder. PASSED live 2026-08-15 on the dev cluster (17.05 s, including the real NTP startup path)
dev_cluster_state: the dev cluster was at manifest 21 (pre-regen) — DdlBootstrap.verifyTables failed on the 3 post-regen tables (fingerprint_dedup, trade_instruction_state, eod_offload_state). The ddl-apply contract REFUSED on the non-empty catalog (exit 3, by design). The 3 tables were created directly with the project's own DdlText parser + raw admin API (the identical descriptor logic DdlApplyTool uses), non-destructive — schema now verifies 24/0/0 and ING-E2E-001 passes end-to-end (fake broker → bridge → IngestionService 5-arg production constructor → Fluss; ticks=324, errors=0)
rollback_behavior: revert FullStackE2ETest.java to the fixed-8s-sleep version (delete the waitForMarker helper + the LOG_DIR env entry + the @TempDir field) and revert the 11-testing-and-release.md ING-E2E-001 row; the 3 dev-cluster tables can be dropped (they hold no required data) to restore the manifest-21 state — no live-cluster code revert needed
plan_tasks: 11-testing-and-release.md (ING-E2E-001 catalog row — harness hardening + live pass)
```
