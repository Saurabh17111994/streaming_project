# CHG-018 — M4 Go-bridge + data-quality hardening: freshness matrix, payload-hash edges, golden pin, plan boundaries, backoff, contract version, counter persistence

```text
change_record_id: CHG-018
scope: test-hardening
owner: Ingestion Team
date: 2026-08-15
affected_artifacts: code/02_services/01_ingestion/src/test/java/com/trading/ingestion/StaleDataTradeGuardTest.java, code/02_services/01_ingestion/src/test/java/com/trading/ingestion/bridge/PayloadHashValidatorTest.java, code/02_services/01_ingestion/src/test/java/com/trading/ingestion/fingerprint/FingerprintBuilderTest.java, code/02_services/01_ingestion/go-bridge/subscription_plan_test.go, code/02_services/01_ingestion/go-bridge/reconnect_test.go, code/02_services/01_ingestion/go-bridge/ndjson_test.go, 15-ingestion-test-hardening.md, 11-testing-and-release.md, 03-ingestion.md, 00-start-here.md, 01-foundation.md
compatibility_class: COMPATIBLE
savepoint_impact: none — test-only additions; no main-code, wire format, state, or DDL change (every M4 item is an "extend existing test" pin of already-shipped behavior; no production code was touched)
test_updates: ingestion 224 -> 231 (+7 M4 — ING-UNIT-014 StaleDataTradeGuardTest.exactBoundaryMatrix 1, ING-UNIT-015 PayloadHashValidatorTest +4, ING-UNIT-016 FingerprintBuilderTest.goldenHashFrozen 1, ING-UNIT-017 FingerprintBuilderTest.dedupSemanticsPinned 1); Go bridge +6 test functions (ING-RES-002 x2 incl. 6 sub-cases, ING-RES-003, ING-RES-004, ING-TCP-003 x2); docs-audit C6 line updated to 340/231/268; full ingestion suite green 231/0/8-skips; Go bridge suite PASS (19 s); no new third-party dependencies (JDK / Go stdlib / unittest only)
rollback_behavior: revert the seven test-file additions (Java +3 files, Go +3 files) and the doc updates (totals 571 -> 564, C6 340/231/268 -> 340/224/268, the 11-testing-and-release.md catalog rows, and the 15-ingestion-test-hardening.md M4 status to backlog); additive test-only change — no live-cluster revert needed
plan_tasks: 15-ingestion-test-hardening.md (M4 milestone — ING-UNIT-014/015/016/017 + ING-RES-002/003/004 + ING-TCP-003 landed)
```

## What landed

**M4 — Go bridge + data quality (plan §3, §2.1, §2.2, §2.6, §2.7):** seven
extend-existing-test items pin behaviors that were implemented but never
asserted. All pure-test additions — no production code changed.

| Test | Pins |
| --- | --- |
| `ING-UNIT-014` — `StaleDataTradeGuardTest.exactBoundaryMatrix` | 7-case freshness walk: receive==broker time, ±1 ms under/over both limits — both exact boundaries (`maxEventAge`, `maxFutureSkew`) are inclusive-FRESH, one ms beyond flips STALE/FUTURE |
| `ING-UNIT-015` — `PayloadHashValidatorTest` +4 | Unpadded base64 accepted (Java's basic decoder is padding-lenient); URL-safe base64 rejected as MALFORMED_PAYLOAD (never silently dropped); 64 KB multi-frame payload round-trips byte-exactly; sha256("") pinned to its real digest (`e3b0c442…`) with the empty payload still a typed rejection (R-186) |
| `ING-UNIT-016` — `FingerprintBuilderTest.goldenHashFrozen` | v1 canonical form frozen: fixture (epoch 0 / token 3045 / 1719000000000 / TRADE / 234500 / 50 / 234400 / 234600) pinned to `aec03d3dc1c556134a8dcdee1d5d94796e9e24d42aed5a1018b57c4e93bc5e31` — any field-order/encoding/null/version change fails the cross-commit pin |
| `ING-UNIT-017` — `FingerprintBuilderTest.dedupSemanticsPinned` | DEC-012 best-effort identity: same tick + same epoch → identical fingerprint (duplicates within an epoch expected — Compute dedups); same tick + next epoch → different (reconnect resets identity) |
| `ING-RES-002` — `subscription_plan_test.go` | Plan-boundary N ∈ {1,511,512,513,1023,1024}: every token exactly once, each request ≤ 512, chunk count = ceil(n/512), union invariant; 1025 on one connection rejected; `validateRequestUnion` fails closed on duplicates (cross/within request), outside tokens, missing tokens, empty and over-size requests |
| `ING-RES-003` — `reconnect_test.go` | `Backoff(attempt)` golden sequence exactly 1,2,4,8,16,30,30… s with NO jitter (deterministic for ING-RES-001 soak accounting); negative attempts clamp to 1 s |
| `ING-RES-004` — `ndjson_test.go` | Every emit path (`tick`, `bridge_event`, `bridge_metrics`) carries `contract_version=2` — schema conformance asserted on each decoded line |
| `ING-TCP-003` — `ndjson_test.go` | 1,024-token counter report: 52 bounded chunks (20/line, each < 603 B truncation bound), total=524800, file + stderr mirror byte-identical; file written FIRST so a dead stderr (SIGPIPE path, simulated with /dev/full) cannot lose the reconcile evidence; `ARROW_TICK_COUNTS_FILE` feeds the report path |

## Verification

- Full ingestion suite: **231 tests, 0 failures, 8 env-gated skips** (was
  224/0/8-skips; +7 M4 Java).
- Go bridge suite: **PASS** (19 s) — includes the 6 new M4 functions.
- docs-audit: C6 counts (340/231/268) PASS; no new env keys, C16 unchanged.
- Python comparator suite untouched.
