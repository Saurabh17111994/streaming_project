# CHG-022 — dedup authority returns to Flink keyed state (Design B); DEC-038 dedup externalization superseded

```text
change_record_id: CHG-022
scope: decision, state-contract
owner: Compute and Strategy Teams
date: 2026-08-17
affected_artifacts: code/02_services/02_compute/src/main/java/com/trading/compute/signaljob/FingerprintDedupFunction.java, code/02_services/02_compute/src/main/java/com/trading/compute/signaljob/SignalJob.java, code/02_services/02_compute/src/main/java/com/trading/compute/signaljob/SignalJobConfig.java, code/01_platform/02_sql/ddl/24_fingerprint_dedup.sql, docs/01_project/04-decisions.md, docs/08_implementation/04-signal-job.md, logs/tracker-14/16-signal-chain-e2e-investigation-20260817.md, docs/05_deployment/change-records/CHG-021.md
compatibility_class: INCOMPATIBLE
savepoint_impact: migration — the fingerprint-dedup operator's state layout changes from a bounded cache (with Fluss-authoritative store + side-output writer) to the authoritative keyed MapState + expiry index; old checkpoints/savepoints of the DEC-038 topology are NOT restorable into the new topology (operator-ID graph also changes with the removed writer/sink vertices). Fresh start / full replay required; no live production checkpoints exist (the E2E is the only consumer and runs env-gated).
test_updates: PENDING — land with the code change (next step); the Design-B snapshot on experiment/design-b-mapstate-dedup @30baa10 already carries the full delta. Planned: FingerprintDedupFunctionTest rewritten to the state-authoritative contract; DedupBucketAssignerTest / FingerprintDedupWriterFunctionTest / FingerprintDedupExternalizationTest / FingerprintDedupExternalizationBenchmarkIT / SigState002RehydrationRestoreIntegrationTest / SigState003FailClosedPreflightIntegrationTest deleted; DedupRocksDbThroughputMemoryIT added (Design-B envelope proof: ~51k/s, checkpoints 0.9→14.4 MB, sub-30 s); SignalJobCompactCheckpointRestoreIntegrationTest re-scoped (checkpoint bounded by TTL, not by cache cap); docs-audit C6 test counts + trio-coherence literals updated at that point
rollback_behavior: revert the dedup operator + SignalJob wiring to the DEC-038 store-backed form and re-add the deleted store/writer classes (all preserved in git history); the fingerprint_dedup DDL is retained (unused) so a revert needs no DDL action; docs revert via the annotate-don't-rewrite markers added by this change
plan_tasks: 04-signal-job.md (dedup design section — superseded to Design B; decision close-out recorded in the investigation log), 04-signal-job.md (E2E re-run on the Design-B topology — §E2E investigation remaining)
```

## What landed and why

The SignalChainLiveE2ETest investigation
(`logs/tracker-14/16-signal-chain-e2e-investigation-20260817.md`, 2026-08-17)
first root-caused the E2E failure to a harness config gap (RocksDB
managed-memory starvation under local execution — CHG-021). After CHG-021's
memory/network passthrough landed, the smokes 8–16 (12:50–14:20) exposed a
SECOND, structural blocker that the harness fixes cannot remove: on the DEC-038
topology the dedup hot path performs a **synchronous Fluss lookup**
(`FlussFingerprintDedupStateStore.lookup` → `lookuper.lookup(...).get(...)`) on
every cache miss, and in every smoke the lookup futures never completed — all 8
`fingerprint-dedup` subtasks parked in `CompletableFuture.get()`, the checkpoint
barrier could not pass, and **checkpoint 1 expired at exactly 30 s → global
restart** (`Checkpoint expired before completing`, zero acks).

The Fluss 0.9.1 client source confirms why the hang is structural for the
DEC-038 design: the client's netty layer has **no timeout on inflight requests**
(``ServerConnection`` TODO), `client.lookup.max-retries` defaults to
`Integer.MAX_VALUE`, and the operator's synchronous `.get()` couples the
checkpoint barrier to an RPC round trip. The native Fluss–Flink connector
(`FlinkTableSource`) *does* offer an async lookup join with native partial
caching — the maintained, non-blocking way to consult Fluss — but a per-record
RPC is the wrong tool for the per-tick dedup hot path regardless.

**Decision (user, 2026-08-17): the fingerprint dedup set is authoritative Flink
keyed state (Design B)** — `MapState<String, DedupEntry>` keyed
`version|token|fingerprint` + `MapState<Long, List<String>>` expiry index +
one event-time timer per entry at `first_seen + DEDUP_TTL_MS`. **CHG-023 item 2
(2026-08-17): the expiry index + event-time timers were then REMOVED — expiry
is native `StateTtlConfig` (300000 ms, `OnCreateAndWrite` + `NeverReturnExpired`,
ProcessingTime) on the MapState; the index/timers were the hand-rolled
replacement for the event-time TTL Flink 2.2.1 lacks, and native TTL is the
correct wall-clock semantics for the 5-minute real-time dedup rule.** This is
Flink's native answer to streaming dedup (no hand-rolled client code), and it
is the topology the smokes proved: 88/88 then 120/120 clean checkpoints, 0 restarts,
feature table grew +124 855 rows, restore ~1.1 s, and the checkpoint-1 zero-ack
stall is structurally impossible (no RPC on the hot path). Fluss remains fully
native on the input (FlussSource) and output (FlussSink) sides — the only role
it ever needs in this job.

| Piece | Detail |
| --- | --- |
| Dedup authority | Flink keyed `MapState` (checkpointed atomically with source offsets); no Fluss store, no query-on-miss, no bounded-cache eviction, no dedup write path |
| Removed machinery | `FlussFingerprintDedupStateStore`, `InMemoryFingerprintDedupStateStore`, `FingerprintDedupStateStore`, `FingerprintDedupWriterFunction`, `DedupBucketAssigner`, the `fingerprint-dedup-writer`/`-sink` vertices, the `fingerprint_dedup` preflight dependency |
| Config keys removed | `DEDUP_STATE_TABLE`, `DEDUP_CACHE_MAX_ENTRIES`, `DEDUP_CACHE_MAX_BYTES`, `DEDUP_WRITE_BATCH_MS`, `DEDUP_WRITE_BATCH_SIZE`, `DEDUP_CLEANUP_INTERVAL_MS` |
| Telemetry removed | `compute.dedup.cache.hits/misses/hit.ratio`, `compute.dedup.rehydration.failures`, `compute.dedup.rehydration.latency.ms` (state-size gauges `state.count`/`expiry.index.count`/`bytes.estimate` kept — **CHG-023 item 2: `expiry.index.count` was then deleted with the index; `state.count` gains a periodic per-token resync to stay honest under native TTL**) |
| DDL fate | `24_fingerprint_dedup.sql` retained on file and in the manifest but no longer a SignalJob dependency (unused); deletion or retention is a separate cleanup decision |
| DEC-038 scope | DEC-038's candle/signal/forming-bar Fluss ownership is UNCHANGED; only its dedup-externalization arm is superseded |

## Trade-off (recorded, not hidden)

The dedup set (~6.1M entries ≈ 628 MB at 20 480 t/s × 300 s TTL) now rides in
the Flink checkpoint instead of living only in Fluss. Evidence that this is
acceptable at the current envelope: `DedupRocksDbThroughputMemoryIT` (~51k/s,
checkpoints 0.9→14.4 MB, durations 20→120 ms) and the E2E's 179 checkpoint-stable
run at p8 + 2048 m managed memory. The "checkpoint must be small" rule
(DEC-038) was born from the pre-RocksDB-incremental incident (1.74 GB heap
state at 53k t/s); RocksDB + incremental checkpoints + managed memory — all
native Flink — remove that constraint. If a future envelope (e.g. 3,000
instruments / 50k t/s) makes the checkpoint too heavy, the native path is the
connector's async lookup join with partial caching, not a return to the raw
client.

## Verification (this record)

- Decision + doc reconciliation verified by `make full-audit` — all layers
  green (exit 0), including the C14 change-control check on this record.
- The three dossiers (04 / 16 / CHG-021) now agree: dedup authority = Flink
  keyed state; DEC-038's dedup arm superseded; candle/signal/forming-bar Fluss
  ownership unchanged.

## Verification (pending — code-landing step, next)

- Merge the Design-B snapshot (`experiment/design-b-mapstate-dedup`, `30baa10`)
  onto the workspace; run the full compute suite; `DedupRocksDbThroughputMemoryIT`
  + `SignalJobCompactCheckpointRestoreIntegrationTest` green (Design-B proof points).
- `SignalChainLiveE2ETest` re-run on the Design-B topology: feature gains rows
  with `window_end` after run start, job RUNNING, checkpoints complete, no
  `Checkpoint expired` — the checkpoint-1 zero-ack stall must not reappear.
