# CHG-023 — native-integration profile: replace custom machinery with the official Flink DataStream + Fluss connector integration

```text
change_record_id: CHG-023
scope: decision, refactor (native-integration profile)
owner: Compute and Strategy Teams
date: 2026-08-17
affected_artifacts: code/02_services/02_compute/src/main/java/com/trading/compute/telemetry/ComputeAlertLogs.java, code/02_services/02_compute/src/test/java/com/trading/compute/telemetry/ComputeAlertLogsTest.java, code/02_services/02_compute/src/test/java/com/trading/compute/telemetry/ComputeAlertLogsLiveDeliveryTest.java, code/02_services/02_compute/src/main/java/com/trading/compute/signaljob/CanonicalSignalFilterFunction.java, code/02_services/02_compute/src/main/java/com/trading/compute/signaljob/FingerprintDedupFunction.java, code/02_services/02_compute/src/main/java/com/trading/compute/signaljob/SignalJob.java, code/02_services/02_compute/src/main/java/com/trading/compute/signaljob/SignalJobConfig.java, code/02_services/02_compute/src/main/java/com/trading/compute/signaljob/TradeDecisionsSinks.java, code/02_services/02_compute/src/main/java/com/trading/compute/signaljob/FormingBarWriterFunction.java, code/02_services/02_compute/src/main/java/com/trading/compute/signaljob/TableContractValidator.java, code/02_services/02_compute/pom.xml, code/common/src/main/java/com/trading/common/config/PlatformConfig.java, code/02_services/01_ingestion/go-bridge/faketool/main.go, code/02_services/02_compute/src/test/java/com/trading/compute/signaljob/FingerprintDedupFunctionTest.java, code/02_services/02_compute/src/test/java/com/trading/compute/signaljob/DedupBaselineMeasurementTest.java, code/02_services/02_compute/src/test/java/com/trading/compute/signaljob/DedupStateSizeTest.java, code/02_services/02_compute/src/test/java/com/trading/compute/signaljob/SignalJobSavepointRestoreIntegrationTest.java, code/02_services/02_compute/src/test/java/com/trading/compute/signaljob/SignalChainLiveE2ETest.java, code/01_platform/04_scripts/o2-provision.py, code/01_platform/04_scripts/run-signal-chain-e2e.sh, docs/08_implementation/04-signal-job.md, docs/08_implementation/11-testing-and-release.md, docs/08_implementation/01-foundation.md, docs/04_contracts/02-storage.md, docs/01_project/04-decisions.md, docs/05_deployment/change-records/CHG-022.md, docs/04_contracts/openobserve.md, docs/06_operations/01-runbooks.md — DELETED 2026-08-17 (item 4): `StallGuardedSink` + `StallGuardedSinkTest` (paths removed from this list — C14 resolves affected_artifacts path tokens against the live tree and the files are deleted, so the `.java` suffix is omitted here; see test_updates)
compatibility_class: INCOMPATIBLE
savepoint_impact: migration — item 2 changes the fingerprint-dedup operator's state layout a second time (keyed MapState + expiry index + event-time timers → keyed MapState + native StateTtlConfig). The Design-B topology (CHG-022) landed in the merge `34af190` (2026-08-17) with no production checkpoints (the E2E is the only consumer and runs env-gated); item 2's TTL wrapper is a seamless 2.2.0+ TTL/non-TTL state migration (legacy plain DedupEntry values wrap with the current processing time at restore). Metric-name changes from item 1 (custom OTLP series → native reporter series) break OpenObserve dashboards until retargeted.
test_updates: ITEM 1 LANDED 2026-08-17 (compute suite 304 run / 0 failures / 16 env-gated skips; C6 truth 340/234/304) — ComputeOtlpEmitter + ComputeOtlpEmitterTest + ComputeOtlpLiveDeliveryTest deleted; the alert-log half is retained as ComputeAlertLogs with ComputeAlertLogsTest (5) + ComputeAlertLogsLiveDeliveryTest; emitter statics removed from RawValidationFunction/FingerprintDedupFunction/CandleLateDrop/SourceIdleWatchdogGenerator and CanonicalSignalFilterFunction rewritten as a RichFilterFunction with a MetricGroup counter; SourceIdleWatchdogGeneratorTest/FingerprintDedupFunctionTest/CandleLateDropTest/DedupBaselineMeasurementTest/CanonicalSignalFilterFunctionTest/RuntimeOptionsTest/CandleTelemetryOutageIntegrationTest reworked to MetricGroup/function-accessor/episode-latch assertions; flink-metrics-otel (2.2.1 fat jar) added to the compute pom and metrics.reporter.otel.* wired in applyRuntimeOptions from OTEL_COLLECTOR_HOST; REQ-FC-010 source-throughput/watermark-lag now flow natively through the FLIP-27 source operator metrics. ITEM 2 LANDED 2026-08-17 (compute suite 302 run / 0 failures / 16 env-gated skips; C6 truth 340/234/302) — FingerprintDedupFunction expiry rewritten to native StateTtlConfig (300000 ms, OnCreateAndWrite + NeverReturnExpired, ProcessingTime) on the keyed MapState; the expiry index (MapState<Long, List<String>>) + per-entry event-time timers deleted; the compute.dedup.expiry.index.count gauge deleted and PER_ENTRY_ESTIMATE_BYTES 128→136 (incl. the 8 B TTL timestamp); the state-count gauge gains a periodic per-token resync (every 10k rows) so invisible native-TTL expiry stays out of it; FingerprintDedupFunctionTest + DedupBaselineMeasurementTest reworked to TTL-clock semantics (harness.setStateTtlProcessingTime past first-seen + TTL re-admits), DedupStateSizeTest.perBucketEstimateCoversExpiryIndex + FingerprintDedupFunctionTest.oneTimerAtSharedExpiryClearsAllEntries deleted, SignalJobSavepointRestoreIntegrationTest javadoc updated, and the dead SIGNAL-warn-dedup-expiry alert retired from o2-provision.py + runbooks. ITEM 4 LANDED 2026-08-17 (compute suite 291 run / 0 failures / 16 env-gated skips; C6 truth 340/234/291) — the StallGuardedSink watchdog deleted (StallGuardedSink.java + StallGuardedSinkTest (11) removed); SignalJob's signal-candidates-current sink and the gated TradeDecisionsSinks machinery unwrapped to plain FlussSinks; PlatformConfig.SINK_WRITE_STALL_TIMEOUT_MS javadoc corrected — the pin now documents its native role as the client.request-timeout passed to every FlussSink (the Fluss client's own request timeout is the stall bound; checkpoint timeout + fixed-delay restart fail the job rather than hang it); runbooks/o2-provision stall-guard references annotated. ITEM 5 RESOLVED-KEEP 2026-08-17 (decision after source verification) — the writer cadence timer + FORMING_BAR_WRITE_BATCH_MS config key are KEPT: FlussSink does NOT coalesce per key (verified in UpsertWriterImpl.upsert — every record handed to the sink becomes its own Fluss log write), so removing the 250 ms coalescing timer would mean per-tick writes at 20 480 t/s (5× log churn) + ~200k records in the sink buffer per 10 s barrier flush — the exact checkpoint-barrier weight class this investigation targeted. The processing-time timer is native Flink timerService API, not un-native machinery; the coalesce-latest-per-instrument design stands. FormingBarWriterFunctionTest UNCHANGED; SignalJobConfig keeps FORMING_BAR_WRITE_BATCH_MS. TableContractValidatorTest UNCHANGED (validator kept as drift guard). E2E PASS 2026-08-17 (SignalChainLiveE2ETest on the final native profile): raw 10 643 609→15 166 519 rows, feature 44 033→65 537 rows (+21 504, 1 024 instruments), 28 checkpoints, 0 restarts, 0 checkpoint-1 stalls, BUILD SUCCESS — the faketool first needed a traded-quantity fix (its full-tick frames never wrote Volume bytes 64:72 / LTQ 12:16, so every tick arrived with last_qty=0 and candles had tick_count=0; fixed in faketool/main.go — qty 1–50 per frame on all three emit paths). Evidence: logs/tracker-14/e2e-final-native-profile-20260817.md. SIG-PERF-001 envelope half DONE 2026-08-17 (test count unchanged at 291/0/16) — SignalChainLiveE2ETest gained the measurement leg (per-sample raw/feature counts + the DESIGN-PERF[envelope] print with throughput + checkpoint evidence): raw +4 559 401 rows, feature +22 440 rows, 1 024 instruments, rawRowsPerSec≈15 125, candleRowsPerSec≈74, 28 checkpoint completions / 0 expired / 0 restarts, BUILD SUCCESS (317.5 s). Evidence: logs/tracker-14/sig-perf-001-envelope-20260817.md. The p99 decision-latency half is signal-side and out of scope (feature-path-only per user 2026-08-17). O2 native-reporter series VERIFIED LIVE 2026-08-17 (closes item 1's verification list) — two real gaps were found and fixed first: (a) the E2E env map never forwarded OTEL_COLLECTOR_HOST (reporter fell back to the container-network default otel-collector:4318 → UnknownHostException every flush; added to the startSignalJob passthrough loop + run-signal-chain-e2e.sh default localhost:4318), and (b) the flink-metrics-otel 2.2.1 shaded OTel SDK passes the endpoint VERBATIM to the HTTP sender (its own default is http://localhost:4318/v1/metrics) — a bare host:port endpoint made the reporter POST to the ROOT, which the otelcol OTLP receiver 404s (probed); SignalJob now appends /v1/metrics. After the fixes the 10-min E2E ran with 0 export failures and the native series landed live in O2 (compute_dedup_state_count 901 128, jvm heap 1.43 GB, jobmanager lastcheckpointduration 5 810 ms); SIGNAL-warn-dedup-state (id 3I2RmqeXtLEGlW0B7rrVta0Ro3R) confirmed bound to stream_name flink_taskmanager_job_task_operator_compute_dedup_state_count (enabled, value > 6500000) and SIGNAL-warn-dedup-expiry confirmed ABSENT; same run PASSED acceptance (raw +6.9M, feature +98 907, 1 024 instruments, 39 samples, 0 restarts). Evidence: logs/tracker-14/o2-native-reporter-series-20260817.md
rollback_behavior: revert to the pre-CHG-023 form — restore the removed classes and the OTLP emitter (all preserved in git history); restore the removed config keys and the expiry-index/timer code; docs revert via the annotate-don't-rewrite markers added by this change
plan_tasks: 04-signal-job.md (native-profile annotations — §Configuration contract, §Required telemetry, §DEC-038 dedup sections; folds the item-2 expiry-mechanism change into the CHG-022 Design-B landing), 04-signal-job.md (verification — compute suite + SignalChainLiveE2ETest re-run on the final native profile)
```

## What landed and why

The user asked for an audit of the signal-job design against the **official
integration surface** — Flink DataStream API + the Fluss connector — with the
rule: "use what they natively provide; do not engineer there." The audit ran
against the actual sources: Flink checkout (`release-2.2.1` tag) and Fluss
checkout (0.9-SNAPSHOT, the 0.9.1-incubating base).

**Every claim below was verified in source, not assumed:**

| Verification | Source evidence |
| --- | --- |
| Flink 2.2.1 ships a native OTLP metric reporter | `flink-metrics/flink-metrics-otel/` present at the `release-2.2.1` tag (FLINK-33696, `OpenTelemetryMetricReporter` + factory; config `exporter.endpoint` / `exporter.protocol` / `exporter.timeout` / `service.name` / `service.version`) |
| Flink has no event-time state TTL (ProcessingTime only) | `StateTtlConfig.java` — enum has only `ProcessingTime` |
| Fluss has no per-key TTL (log TTL only) | `ConfigOptions.java` — only `TABLE_LOG_TTL`; no key-level TTL in the server |
| Fluss lookup client retries forever by default | `ConfigOptions.java` — `client.lookup.max-retries` default `Integer.MAX_VALUE`; `ServerConnection.java` inflight-limit TODO |
| Fluss sink natively batches + flushes on the checkpoint barrier | `FlinkSinkWriter.flush` / `UpsertSinkWriter` via the connector Sink API |
| Connector open-time validation is minimal | `FlinkSinkWriter.initialize` — table exists + PK-presence match only; `FlinkSourceEnumerator.start` — table exists only |
| Fluss DataStream source/sink are the official API | `FlussSourceBuilder`/`FlussSource`, `FlussSinkBuilder` |

**Decision (user, 2026-08-17): adopt the native-integration profile — replace
the five pieces of custom machinery below with what Flink/Fluss natively
provide.** `TableContractValidator` is explicitly **kept** (see below) — the
connector's open-time checks are only existence + PK-presence, so the validator
is a live-cluster drift guard, not duplication.

## The five swaps

| # | Not native (current) | Native replacement (verified available) | Delete / change |
| --- | --- | --- | --- |
| 1 | `ComputeOtlpEmitter` — hand-rolled OTLP/JSON exporter + payload-shape tests | Flink 2.2.1 native reporter `flink-metrics-otel` (jar on the compute classpath; reporter config `exporter.endpoint` → OpenObserve OTLP endpoint, `exporter.protocol` per receiver) | Delete the emitter + its tests; all existing Flink counters/gauges flow out natively; retarget OpenObserve dashboards (openobserve.md, runbooks) |
| 2 | Dedup expiry — per-entry event-time timers + expiry index (`expiry → keys`) in `FingerprintDedupFunction` (+ the eviction-timer-deletion fix and its regression test) | Native `StateTtlConfig` (300000 ms, `NeverReturnExpired`) on the keyed `MapState` — removes the index (own budget table: +100–150% state overhead), the timers, and the orphaned-timer bug class | Rewrite `FingerprintDedupFunction` expiry to TTL; delete index/timer code + tests; state ~halves |
| 3 | Raw-client Fluss machinery (DEC-038 era): `FlussFingerprintDedupStateStore`, `InMemoryFingerprintDedupStateStore`, `FingerprintDedupStateStore`, `FingerprintDedupWriterFunction`, `DedupBucketAssigner`, full-row `delete` cleanup | Already superseded by Design B (CHG-022) — the native DataStream answer is keyed state; finish the deletion (overlaps CHG-022's removal list) | Delete the five classes + tests + `fingerprint_dedup` DDL dependency |
| 4 | `StallGuardedSink` watchdog — a patch for the Fluss client hang (retry=∞, no inflight limit) | Under Design B there is no dedup write path; output sinks are plain `FlussSink`s — write batches carry `client.request-timeout` (default 30 s), and the NATIVE stall-guard is the checkpoint timeout (30 s) + fixed-delay restart failing the job, never hanging it | **LANDED 2026-08-17** — wrapper + test deleted; SignalJob + TradeDecisionsSinks unwrapped to plain `FlussSink`s |
| 5 | Writer cadence-flush — `FORMING_BAR_WRITE_BATCH_MS`, `DEDUP_WRITE_BATCH_MS/SIZE` flush timers double-batching on top of the connector | The native `FlussSink` already buffers and flushes on the checkpoint barrier | **RESOLVED-KEEP 2026-08-17** — `FlussSink` does NOT coalesce per key (verified in `UpsertWriterImpl.upsert`), so the cadence timer is load-bearing at 20 480 t/s: it turns the per-tick snapshot stream into ~4 096 coalesced upserts/s. Removing it = per-tick writes (5× churn) + ~200k buffered records per barrier flush. The processing-time timer is native Flink `timerService` API. `FORMING_BAR_WRITE_BATCH_MS` + the coalescing writer stay; `DEDUP_WRITE_BATCH_MS/SIZE` are already gone with the DEC-038 dedup store (CHG-022) |

**Kept as-is — `TableContractValidator` (drift guard, not over-engineering):**
the connector's open-time validation was traced end-to-end:
`FlinkSinkWriter.initialize` checks table existence (`getTable`) + PK *presence*
(`sanityCheck`), and *tolerates* column-count mismatch (schema-evolution
`ProjectedRow` mapping); `FlinkSourceEnumerator.start` checks table existence
only. It does **not** check exact PK column set, bucket count, `bucket.key`,
strict column count, or `kv.format-version` — all of which the validator checks,
and all of which have caught real live drift (the id-7 LOG-era forming_bar
table, the id-90 LOG→KV candle conversion, the id-693→697 TTL recreate). The
validator stays; only the table-existence leg is arguably redundant, and it is
retained as intentional pre-submit fail-fast.

## Trade-offs (recorded, not hidden)

- **Expiry semantics change (item 2):** native TTL is wall-clock
  (ProcessingTime), not watermark-based. For a 5-minute real-time dedup rule
  this is the *correct* semantics; the event-time machinery was both non-native
  and unnecessary. If a future requirement genuinely needs watermark-aligned
  expiry, the native fallback is event-time timers *without* the index (one
  timer per key, direct delete) — recorded, not built.
- **Metric series change (item 1):** OpenObserve dashboards/alerts keyed on the
  custom emitter's series must retarget to the native reporter's series in the
  same change (openobserve.md + runbooks list the affected rows). Semantics
  note: the native reporter exports counters as **monotonic DELTA** sums (the
  standard OTel counter shape); the custom emitter emitted `isMonotonic=false`
  — dashboards on those series must be re-created with the monotonic flag.
- **Checkpoint size (item 2):** removing the expiry index shrinks the
  checkpoint further — the Design-B envelope (~628 MB at 20 480 t/s) already
  proved acceptable; this only improves it.

## Verification (this record)

- Decision + doc reconciliation verified by `make full-audit` — all layers
  green (exit 0), including the C14 change-control check on this record.

## Verification (all five items resolved 2026-08-17; E2E PASSED)

- **Item 1 landed 2026-08-17:** emitter → native `flink-metrics-otel` reporter
  (pom dep + `metrics.reporter.otel.*` wiring in `applyRuntimeOptions` from
  `OTEL_COLLECTOR_HOST`; emitter statics removed from the five operators; the
  alert-log half retained as `ComputeAlertLogs`).
- **Item 2 landed 2026-08-17:** `FingerprintDedupFunction` expiry → native
  `StateTtlConfig` (300000 ms, `OnCreateAndWrite` + `NeverReturnExpired`,
  ProcessingTime) on the keyed `MapState`; the expiry index + per-entry
  event-time timers + the `compute.dedup.expiry.index.count` gauge are
  deleted; the state-count gauge gains a periodic per-token resync (10k rows)
  to stay honest under invisible TTL expiry; background physical cleanup is
  native (RocksDB TTL compaction filter, compatible with the job's
  incremental checkpoints). Full compute suite green (302 run / 0 failures /
  16 env-gated skips); C6 truth 340/234/302; `make full-audit` green.
- Items 4 + 5 resolved 2026-08-17: item 4 LANDED (`StallGuardedSink`
  deleted — plain `FlussSink`s, suite 302 → 291, C6 340/234/291); item 5
  RESOLVED-KEEP (FlussSink does not coalesce per key, so the 250 ms
  forming-bar cadence timer stays — the timer is native Flink
  `timerService` API, and removing it would mean per-tick writes at
  20 480 t/s). All five items now resolved (1–4 landed, 5 keep).
- **E2E PASSED 2026-08-17 on the final native profile:** `SignalChainLiveE2ETest`
  live run (5 min, faketool, dev Fluss) — raw 10 643 609 → 15 166 519 rows,
  feature 44 033 → 65 537 rows (+21 504 across 1 024 instruments), 28
  checkpoints complete, 0 restarts, **0 checkpoint-1 zero-ack stalls** (the
  investigation's stall is gone on the final profile), job RUNNING through all
  19 samples, `tick_count >= 1` / OHLC / schema-v2 assertions all pass.
  Pre-requisite fix landed in the same session: the faketool full-tick frames
  now carry a traded quantity (Volume bytes 64:72 + LTQ 12:16) — without it
  every tick arrived with `last_qty=0` and candles had `tick_count=0`
  (evidence `logs/tracker-14/e2e-final-native-profile-20260817.md`).
- **OpenObserve verification DONE 2026-08-17 (native series live, after two
  real fixes):** the earlier "series flowed" claim was WRONG — that run's
  reporter hit `UnknownHostException: otel-collector` every flush (the E2E
  env map never forwarded `OTEL_COLLECTOR_HOST`; fixed via the passthrough
  loop + script default `localhost:4318`), and the endpoint needed the
  `/v1/metrics` signal path (flink-metrics-otel 2.2.1 passes the endpoint
  verbatim to the HTTP sender — a bare host:port POSTs to the ROOT, which the
  otelcol OTLP receiver 404s; SignalJob now appends the path). After the
  fixes, a 10-min E2E ran with 0 export failures and the native series landed
  live in O2 (`flink_taskmanager_job_task_operator_compute_dedup_state_count`
  901 128, jvm heap 1.43 GB, jobmanager lastcheckpointduration 5 810 ms);
  `SIGNAL-warn-dedup-state` (id `3I2RmqeXtLEGlW0B7rrVta0Ro3R`) confirmed on
  the native `stream_name` (enabled, `value > 6500000`) and
  `SIGNAL-warn-dedup-expiry` confirmed ABSENT; dashboards carry the
  monotonic-DELTA flag. Evidence
  `logs/tracker-14/o2-native-reporter-series-20260817.md`;
  `make full-audit` green.
