# CHG-067 — Task C2: config parity for ARROW_HFT_CONNECTIONS 1..3

```text
change_record_id: CHG-067
scope: ingestion-bridge, ingestion-config, hft-policy, requirement
owner: Saurabh (solo operator, DEC-044)
date: 2026-08-21
affected_artifacts: code/02_services/01_ingestion/src/test/java/com/trading/ingestion/config/ConfigParityTest.java, code/02_services/01_ingestion/src/main/java/com/trading/ingestion/config/IngestionConfig.java, code/02_services/01_ingestion/go-bridge/hft_policy_test.go, code/02_services/01_ingestion/go-bridge/main.go, logs/nautilus-execution/c2-config-parity-20260821.md
compatibility_class: COMPATIBLE
savepoint_impact: none — widened validation range (default 1 unchanged); no schema, state, savepoint, or checkpoint format touched. Existing deployments with ARROW_HFT_CONNECTIONS=1 or unset see zero behavior change; out-of-range values (0, 4, abc) now correctly rejected with documented FATAL.
test_updates: updated ConfigParityTest.java TABLE (CONNECTIONS pinned 1 → tunable 1..3) and IngestionConfig.java (exactInt → intRange 1..3); updated hft_policy_test.go table and main.go (hftPin → hftRange); existing parity tests now exercise accepted (1..3) and rejected (0,4,abc) symmetrically — Java BUILD SUCCESS, Go PASS (18.6s), Rust 153/0 unaffected
rollback_behavior: revert the four lines to pinned 1 (exactInt/hftPin); bridge and ingestion revert to single-socket only; no state to migrate
plan_tasks: docs/plans/2026-08-21-live-readiness-master-plan.md (Task C2), 19-nautilus-execution-service-implementation-plan.md
```

## Summary

Closes C2: lifts `ARROW_HFT_CONNECTIONS` from pinned `1` to tunable `1..3`
on both sides of the Java↔Go parity contract (`ING-UNIT-018`):

* Go: `hft_policy_test.go` table `1,1,1,1` → `-1,1,1,3`, `main.go`
  `hftPin(1)` → `hftRange(1,1,MaxHFTConnections)`.
* Java: `ConfigParityTest.java` table `1,1,1,1` → `-1,1,1,3`,
  `IngestionConfig.java` `exactInt(1)` → `intRange(1,1,3)`.

Both sides now reject `0`, `4`, and `abc` identically and accept `1..3`.
`ARROW_HFT_LATENCY_MS` and other HFT keys remain unchanged and in lockstep.

## Evidence

`logs/nautilus-execution/c2-config-parity-20260821.md` — table of changes,
parity verification (Java BUILD SUCCESS, Go PASS).
