# CHG-085 — Task D2.1 first half: `make stack-selfcheck` passes (real daemon, single-node swarm mimic)

```text
change_record_id: CHG-085
scope: deployment, swarm
owner: Saurabh (solo operator, DEC-044)
date: 2026-08-21
affected_artifacts: CHG-085.md, docs/plans/2026-08-25-live-readiness-unified-plan.md, logs/nautilus-execution/d2-stack-selfcheck-20260821.md
compatibility_class: COMPATIBLE
savepoint_impact: none — read-only stack validation (swarm init/label + `docker stack config`); the script's DOWN=1 teardown removed the (previously absent) `prod` stack; no persisted state change.
test_updates: make test-09 static stack coverage 25/25 PASS (test_09_stack.py); `make stack-selfcheck` rc=0 on this host against a live docker daemon — swarm initialized, node labelled role=worker + observability=true, `docker stack config` compiles docker-stack.yml. Also re-ran as `make stack-config` compile-only path (same script).
rollback_behavior: n/a — validation-only; swarm labels are idempotent, stack was not left deployed (DOWN=1 default).
plan_tasks: docs/plans/2026-08-25-live-readiness-unified-plan.md (Task D2)
```

## Summary

Completes the first half of `D2.1` ("`make stack-selfcheck` (single-node mimic)
passes") on a live docker daemon:

* `docker stack config -c docker-stack.yml` compiles the production manifest
  (schema/deploy errors would fail here); the single node is labelled with the
  same placement constraints the real 7-VM cluster uses (`role=worker`,
  `observability=true`), so the stack's role-based placement provably schedules
  on this mimic.
* Static offline coverage: `make test-09` = 25/25.

## Remaining (honest)

* The **second half of D2.1** — `make stack-config DEPLOY=1` on the real swarm —
  remains 🔒 until `D1.3` (human provisions the 7 VMs and `prod_node_check.py`
  passes). This record is single-node mimic evidence only; multi-VM HA
  (SWARM-MGR-* quorum) is M3, untouched by this pass.
