# CHG-030 — live-gated compute fixes: Kryo/JDK-17 argLine, UID contract 10→14, forming-bar-scoped assertions (CHG-029 findings resolved)

```text
change_record_id: CHG-030
scope: test/build config + live-test assertion scoping — no production runtime code changed (SignalJob topology and state untouched); resolves the four CHG-029 root causes
owner: Compute (Signal Job) Team
date: 2026-08-18
affected_artifacts: code/02_services/02_compute/pom.xml (surefire argLine + java.util.concurrent.atomic add-opens), code/02_services/02_compute/src/test/java/com/trading/compute/signaljob/SignalJobOperatorUidTest.java (EXPECTED_OPERATORS 10 → 14), code/02_services/02_compute/src/test/java/com/trading/compute/signaljob/CandleGraphReplayIntegrationTest.java (window-driven scoping + KV last-upsert scoping), code/02_services/02_compute/src/test/java/com/trading/compute/signaljob/CandleFailureInjectionIntegrationTest.java (window-driven scoping), code/02_services/02_compute/src/test/java/com/trading/compute/signaljob/CandleRocksDbRestoreIntegrationTest.java (window-driven scoping), docs/05_deployment/change-records/CHG-029.md (root-cause 3 correction + root-cause 4 isolation + status update)
compatibility_class: COMPATIBLE
savepoint_impact: none — no topology, operator, or state change. The UID reconciliation (pinning the 4 forming-bar operators added 2026-08-16) RESTORES the savepoint-anchor contract (CHECKPOINT-RESTORE-001): the pinned set now matches the 14-operator graph instead of drifting by 4.
test_updates: fixes applied and verified by a chunked full-gated live re-run (2026-08-18, dev Fluss stack up, all 11 gates on): chunk 1 safety/alert/signal-KV 3/3, chunk 2 checkpoint/savepoint/restore 4/4, chunk 3 rehydration/rocksdb/telemetry/UID 6/6, chunk 4 object-store/dedup-rocksdb 2/2, chunk 5 SignalChainLiveE2ETest 1/1 (full 30-min run, BUILD SUCCESS). Default suite restored to the canonical 292 run / 0 failures / 17 skipped; docs-audit, test-audit-r2 (119 OK), and stale-table scan all green.
rollback_behavior: revert the 5 test/build files — the argLine add-opens is a test-JVM-only flag (never applied to the production job); the assertion scoping is test-only. CHG-029 remains a findings-only record.
plan_tasks: 04-signal-job.md (CHG-029 live-gated test findings — all four root causes fixed and verified by CHG-030)
```

## What happened

CHG-029 (2026-08-18) documented 6 latent failures that surfaced when the env-gated live
compute tests first ran on the current topology/JDK: a Kryo/JDK-17 surefire argLine gap (4
failures), a stale pinned-UID contract (1), a faketool path-resolution error (1), and one
un-isolated watermark-stall assertion. This record applies and verifies the four fixes.

## Fixes

**1. Kryo/JDK-17 argLine gap (CHG-029 root cause 1).**
Added `--add-opens=java.base/java.util.concurrent.atomic=ALL-UNNAMED` to the compute surefire
`argLine` (the existing block already opened `java.nio` and `java.util`). Any live job that
serializes Flink `BinaryRowData` via the Kryo fallback (candle output through the late-drop
side-output) crashed with `InaccessibleObjectException` on `AtomicBoolean.value`; the add-opens
clears the module-access wall for the whole fork. Test-JVM-only — production job config
untouched.

**2. Pinned-UID contract 10 → 14 (CHG-029 root cause 2).**
`SignalJobOperatorUidTest.EXPECTED_OPERATORS` now pins the 4 forming-bar operators added by
Slice 2.2 on 2026-08-16 — `forming-bar-builder`, `forming-bar-detection`, `forming-bar-writer`,
`forming-bar-sink` — matching the live 14-operator graph (verified: every operator carries a
UID that equals its `.name()`). This closes the CHECKPOINT-RESTORE-001 savepoint-anchor drift.

**3. E2E faketool path resolution (CHG-029 root cause 3 — no code change).**
Confirmed the binary exists; the failure was the test's RELATIVE `FAKETOOL_BIN` default
resolved against the surefire fork's cwd. The canonical runner
(`run-signal-chain-e2e.sh`) always exports an absolute `FAKETOOL_BIN` plus the computed
`INGESTION_CLASSPATH`. The full E2E was run through the runner (30-min, faketool broker) and
passed — no fix code needed; CHG-029's root-cause 3 was corrected to state this.

**4. Forming-bar-scoped assertions — option (b) (CHG-029 root causes 4 + 5).**
Since Slice 2.2 (2026-08-16) the tick-driven forming-bar placeholder detector
(`CANONICAL_FORMING_RULE_ID`) fires candidates into the same signal LOG/KV sinks as the
window-driven breakout rule (`CANONICAL_RULE_ID`). Three P6/P4.3 tests counted EVERY LOG row in
their `candidateIds`/signal-count helpers, so their pre-forming-bar expectations broke
(replay: 36 vs 2; stall: 5→3 vs 0; rocksdb restore: 30 vs 2). Per user decision (option b),
the assertions now scope to the window-driven rule only:

- `CandleGraphReplayIntegrationTest` — `signalLogRows`/`candidateIds` filter to
  `CANONICAL_RULE_ID`; the KV current-state comparison is scoped to the two canonical w22
  candidates (the window-driven id at w22 end or the forming-bar id fired mid-w22) with a
  settle-before-read helper, because the two producers union into the KV sink and the final
  last-upsert per token is interleaving-dependent — the assertion pins convergence (same keys,
  canonical w22 content) instead of a non-deterministic order.
- `CandleFailureInjectionIntegrationTest` — `candidateIds` filters to `CANONICAL_RULE_ID`;
  the flat-window stall assertion now reads "no window-driven (breakout) signal".
- `CandleRocksDbRestoreIntegrationTest` — `candidateIds` filters to `CANONICAL_RULE_ID`.

The forming-bar branch itself is unchanged and covered by its own unit tests
(`FormingBarDetectionFunctionTest` et al.).

## Verification

- **Chunked full-gated live re-run (2026-08-18, all 11 gates on, R2 creds from the docker
  `.env`):** chunk 1 3/3, chunk 2 4/4, chunk 3 6/6, chunk 4 2/2, chunk 5
  `SignalChainLiveE2ETest` 1/1 — full 30-min run, BUILD SUCCESS (1824 s; fresh feature
  candles emitted after the ~101M-row raw backlog drained, feature 469 572 → 836 876+).
- **Targeted pre-checks:** `SignalJobOperatorUidTest` pass; `CandleFailureInjectionIntegrationTest`
  2/2 pass; `CandleGraphReplayIntegrationTest` 3/3 pass; `CandleRocksDbRestoreIntegrationTest`
  pass after the scoping fix.
- **Gates:** docs-audit all checks pass (C6 compute count matches the canonical 292 after the
  stale `*IT` report artifact was removed); test-audit-r2 119 OK; stale-table scan 0
  unannotated, exit 0.

## Evidence

- `/tmp/e2e-full.log` — 30-min E2E run, samples showing raw growth and the feature-count jump
  once the backlog drained; `Tests run: 1, Failures: 0, Errors: 0` / BUILD SUCCESS.
- Fresh compute surefire reports (2026-08-18 11:47–12:41) for the chunked runs, then the
  canonical plain-suite set (292/0/17).
- Before/after failure deltas captured in the session: UID 14-vs-10 → pass; replay 36-vs-2 and
  KV order race → pass; stall 5→3-vs-0 → pass; rocksdb 30-vs-2 → pass; Kryo
  `InaccessibleObjectException` crash → gone.

## Status

- **All four CHG-029 root causes fixed and verified; no production runtime code changed.**
- The default suite remains at the canonical 292 run / 0 failures / 17 skipped; the 17 skips
  are the env-gated live tests now proven green when enabled.
- Next: E2E backlog housekeeping — with ~101M rows in dev `raw_table_1`, each full E2E run
  needs ~30 min to drain to the tail before fresh candles flow; a dev-table reset would make
  bounded E2E runs feasible again (separate follow-up, requires user decision).
