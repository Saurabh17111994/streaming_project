<!-- markdownlint-disable MD013 -->

# Arrow Broker Contract

Source of truth for the external Arrow Trade APIs the platform integrates with.
Authority: this document > prose in `01_project`/`02_requirements`. Code in
`code/common/broker` and `code/common/arrow` must follow the schemas here.

Raw sources reviewed (not in this repo):

- `Arrow_broker/go-arrow` (Go SDK: `arrow/orders.go`, `constants.go`, `streams.go`, `hft_stream.go`)
- `Arrow_broker/rest-api` (docs mirror: `orders`, `market-data`, `hft-stream`, `authentication`, `response-structure`)
- `Arrow_broker/instruments/cash_stocks/NSE_CM_EQUITY.csv` (token→symbol map; full NSE cash), and the current testing-phase manifest `Arrow_broker/instruments/cash_stocks/NSE_CM_EQUITY (1024).csv` (1,024 unique NSE cash tokens, quoted CSV); `BSE_CM.csv` (token→symbol map)

## Identity mapping (SAFETY CRITICAL)

The platform uses three distinct order identities (no generic `order_id`):

| Platform identity | Arrow field | Notes |
| --- | --- | --- |
| `instruction_id` | _(internal only)_ | Generated by Signal job; never sent to broker |
| `client_order_ref` | `remarks` (request) / `remarks` (order book) | **Max 16 chars.** Round-trips our ref back to us |
| `broker_order_id` | `orderNo` (place response) / `id` (order book) / `id` (trade book) | Broker-assigned order number |
| `exchange_order_id` | `exchangeOrderID` | Exchange-assigned (informational) |

**Decision (foundation):** `client_order_ref` MUST be ≤ 16 characters. Enforced by
`arrow/ArrowOrderRequest.java`. Recommended encoding: 3-char prefix + 13-char
base36 (e.g. `INS` + 13 chars). Confirm if a different compact form is preferred.

**Instrument join key:** `instrument_token` (numeric) = Arrow `Token` (market feed,
order book, trade book) = `Token` column in the cash_stocks CSV. This is the
`bucket.key` for `01_ticks_raw` and the link across market/postback/order books.

## 1. Authentication (`broker_market`, `broker_postback`, `arrow_rest` share it)

OAuth-style token exchange. Credentials: `appID` + `appSecret` (never exposed client-side).

1. Login redirect: `https://app.arrow.trade/app/login?appID=<appID>` → user enters User ID + Password + TOTP.
2. Redirect returns `request-token` + `checksum` = SHA256(`request-token:appID`).
3. Exchange: `POST https://edge.arrow.trade/auth/app/authenticate-token`
   body `{ "checkSum": SHA256("appID:appSecret:request-token"), "token": <request-token>, "appID": <appID> }`
   → `{ "data": { "name", "token": <ACCESS_TOKEN>, "userID" }, "status": "success" }`
4. All API + WebSocket calls use headers `appID` + `token`.

**Non-interactive alternative (verified 2026-08-13):** `Client.AutoLogin(userID,
password, totpSecret)` completes the same flow programmatically — no request
token, no redirect. The Go SDK's login step POSTs to
`https://api.arrow.trade/auth/validate-2fa` with `{code, requestId, userID}`
after the initial login (body: `userID`, `password`, `captchaValue`,
`captchaID`, `appID`, `isAppLogin`); the exchange then POSTs to
`https://edge.arrow.trade/auth/app/authenticate-token` with the same
`checkSum` (SHA256 `appID:appSecret:request-token`). Requires the same
`appID` + `appSecret` credentials (TOTP secret base32-decoded, standard 30 s
window). Python client `auto_login(user_id, password, api_secret,
totp_secret)` mirrors this flow.

**Constraints:**

- Access token lifespan **24h** (regulatory). No refresh token by default — executor must re-authenticate before expiry.
- Static IP is mandatory (SEBI circular, Feb 2025).

## 2. Market data feed (`broker_market`)

Two WebSocket feeds. Both carry `Token` (int32) as the instrument key.

### 2a. Standard Data Stream — `wss://ds.arrow.trade?appID=&token=`

- Binary, **big-endian**. Modes: `ltp` (13B), `ltpc` (17B), `quote` (93B), `full` (249B — verified live 2026-08-13, BROKER-MD-001; 241B legacy layout still parsed: depth at 101, current wire has 8 reserved bytes at 101:109, depth at 109).
- Subscribe: JSON `{ "code": "sub", "mode", "<mode>": [tokens] }`. Unsub: `code: "unsub"`.
- Prices are integer-scaled (paise, ×100) for NSE/BSE equities.
- `full` depth: 5-level bid/ask (qty int64, price int32, orders int16).

### 2b. HFT Data Stream — `wss://socket.arrow.trade?appID=&token=&zstd=1`

- Binary, **little-endian**, **zstd-compressed inbound** (mandatory from **8 July 2026**).
- Modes: `ltpc` (40B), `full` (196B). Tick interval `latency` 50ms–60000ms (default 1000).
- Subscribe by symbol (`NSE.SBIN-EQ`) or token IDs (`symIds: [{exch_seg, ids}]`).
- Prices in **paise** (×100); timestamps in **nanoseconds** (unix).
- Per connection: ≤1024 symbols; ≤512 per subscription request.
- **Tier scope:** basic tier = 1 WebSocket connection; premium tier = 3 connections. The current testing phase uses the basic tier (1 connection) with the 1,024-instrument manifest. The 3-connection / 3,000-instrument coverage is the deferred future production target and requires account capability evidence before activation.
- **Recommendation (foundation):** use HFT feed for the tick path (low-latency, relevant to trading). Standard feed is fallback.

**Verification flags:** full-mode byte size = 249B live (resolved 2026-08-13, BROKER-MD-001; paise scaling confirmed on both feeds against a live sample — RELIANCE ltp 131700 identical standard/HFT). After ~15:15 IST every mode appends a 16-byte Closing Auction Session trailer (29/33/109/265; imbalance_qty i64 + indicative_close i32 + ref_price i32).

## 3. Order API (`arrow_rest`)

Base `https://edge.arrow.trade`. All calls: `appID` + `token` headers.

### `POST /order/regular`

Request (all strings except `mpp` bool):
`exchange` (NSE/BSE/NFO/BFO/MCX…), `symbol`, `quantity`, `transactionType` (`B`/`S`),
`order` (`LMT`/`MKT`/`SL-LMT`/`SL-MKT`), `product` (`I`/`C`/`M`), `price` (use `"0"` for MKT),
`validity` (`DAY`/`IOC`), `disclosedQty`, `remarks` (**≤16 chars = client_order_ref**), `mpp` (bool).

Success: `{ "data": { "orderNo": "<broker_order_id>", "requestTime" }, "status": "success" }`.

### Modify / Cancel

`PATCH /order/regular/<orderID>`, `DELETE /order/regular/<orderID>`.

### Order book `GET /user/orders`, Order `GET /order/<id>`

Each row: `id` (= `broker_order_id`), `exchangeOrderID`, `symbol`, `token`, `orderStatus`
(`PENDING`/`OPEN`/`COMPLETE`/`CANCELLED`/`REJECTED`), `reportType`
(`NewAck`/`PendingNew`/`Fill`/`Canceled`/`Rejected`), `fillShares`, `averagePrice`,
`remarks` (= our `client_order_ref`), `pricePrecision`, `tickSize`, `lotSize`, `rejectReason`.

### Trade book `GET /user/trades`

Each row: `id` (order id), `exchangeOrderID`, `fillID`, `fillPrice`, `fillQuantity`,
`fillTime` (epoch sec), `token`, `averagePrice`, `remarks`.

**Constraints:**

- **MKT orders disabled by default.** Use `LMT`, or `mpp: true` to mimic market at upper limit / DPR.
- Order types: `LMT`, `MKT`, `SL-LMT`, `SL-MKT`. Products: `I` (intraday), `C` (delivery), `M` (F&O).
- Idempotency: use unique `remarks` (client_order_ref) to prevent duplicate submission.

## 4. Order update / postback feed (`broker_postback`)

`wss://order-updates.arrow.trade?appID=&token=` — JSON text frames (order lifecycle updates).
Shape mirrors OrderDetails: `id`, `orderStatus`, `reportType`, `fillShares`, `averagePrice`,
`exchangeOrderID`, `remarks` (= `client_order_ref`), `token`, `rejectReason`, `exchangeUpdateTime`.
This is the **Action Capture** source for the `Fills` table.

## 5. OpenObserve (`openobserve`) — NOT PROVIDED

No contract supplied. Row stays `TO_BE_VERIFIED` in `version_matrix.yaml`. Provide
version + ingestion protocol (OTLP / HTTP JSON) to pin.

## Capability tests that verify this contract

- `BROKER-MD-001/002` — parse standard + HFT binary ticks (token + price join).
- `BROKER-PB-001` — parse order-update JSON → `Fills` mapping.
- `ARROW-REST-001` — build `/order/regular` request with `remarks`=client_order_ref(≤16), parse response `orderNo`.
- See `docs/08_implementation/12-version-compatibility-evidence.md`.
