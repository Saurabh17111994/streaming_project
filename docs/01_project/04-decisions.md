# Architectural Decisions

This file indexes active project decisions. Detailed implementation belongs in contracts, DDLs, and code. A revised decision explicitly supersedes older wording rather than leaving both active.

## Active decisions

| ID      | Decision                                                                                                                                                                                                                                       | Reason                                                                                                                                              | Trade-off                                                                                                 |
| ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| DEC-001 | Fluss is the live streaming bus and storage layer. LOG tables hold immutable events; KV tables hold current operational state.                                                                                                                 | One data plane and native changelog consumption.                                                                                                    | Requires strict table and column ownership.                                                               |
| DEC-002 | Flink performs candle computation, signal detection, ranking, and babysitting. (**Ranking REMOVED 2026-08-15, CHG-005 — historical decision wording retained.**)                                                                                                                                                                 | Stateful event-time processing and checkpointed recovery.                                                                                           | Operational complexity is higher than stateless services.                                                 |
| DEC-003 | Ingestion decodes Arrow's documented protocol and appends through the supported Fluss Java client path.                                                                                                                                        | Arrow has a Go SDK (`github.com/arrow-trade/go-arrow`) but Fluss has no supported Go writer; the Java client path is the only supported write path. | The custom decoder must be compatibility-tested against captured broker packets.                          |
| DEC-004 | There are two Flink jobs: a feature-computation + signal-detection + in-operator-ranking job, and a separate position-babysitting job. (**In-operator ranking REMOVED 2026-08-15, CHG-005 — historical decision wording retained.**)                                                                                                         | Avoids a candidate round-trip and ranking-window delay while retaining a separate position-management failure domain.                               | There is no separate Ranking deployment.                                                                  |
| DEC-005 | KV tables use explicit writer/column ownership and `partial_update` where supported.                                                                                                                                                           | Prevents unrelated column clobbering.                                                                                                               | Does not guarantee external exactly-once behavior or solve stale writes within one owner.                 |
| DEC-006 | The Executor consumes Fluss, owns the durable order gate and reconciliation, and calls the Arrow REST API directly (`POST /order/regular`). There is no intermediate OpenAlgo layer. **(Order-path wording SUPERSEDED 2026-08-18 by DEC-041/CHG-027 — the go-arrow bridge, not a direct Executor call, is the only path to Arrow REST; the no-third-party-OpenAlgo policy stands.)**                                                                                | Places safety and idempotency in one component. Arrow's native REST API eliminates an unnecessary adapter.                                                                    | Executor state must be durable and highly observable.                                                                                                                                   |
| DEC-007 | Order correlation uses `instruction_id`, `client_order_ref`, and `broker_order_id`.                                                                                                                                                            | Removes identity ambiguity and supports one instruction producing multiple broker orders.                                                           | Requires a durable mapping.                                                                               |
| DEC-008 | Eligible immutable data is offloaded to Iceberg/S3 at EOD.                                                                                                                                                                                     | Avoids live-hour S3 contention.                                                                                                                     | Fluss retention must provide a multi-day retry buffer and expiry must follow verified offload completion. |
| DEC-009 | OpenObserve provides operational logs, metrics, traces, and alerts.                                                                                                                                                                            | One observability backend.                                                                                                                          | Business analytics remains out of MVP scope.                                                              |
| DEC-010 | Local development uses Docker Compose; production uses four-VM Docker Swarm.                                                                                                                                                                   | Keeps local setup simple while retaining a production HA target.                                                                                    | The two environment definitions must not be mixed.                                                        |
| DEC-011 | The data path auto-recovers; the order path uses a durable safe-halt gate and manual reconciliation before resume.                                                                                                                             | Missing a trade opportunity is safer than duplicating or placing a stale order.                                                                     | —                                                                                                         |
| DEC-012 | Tick identity is a bounded best-effort event fingerprint; Arrow's binary market-data WebSocket provides no broker-global sequence or event ID in any subscription mode (HFT LTPC/Full; the Standard feed carrying LTP/Quote was removed 2026-08-14). | Confirmed by Arrow Go SDK and REST API docs (Evidence Level 1). Hash collisions and identical legitimate ticks remain a documented limitation. | Evidence-gated at Level 1 only — sandbox capture (Level 2) and failure-mode testing (Level 3) required before live money. |
| DEC-013 | Order lifecycle and position lifecycle are separate aggregates.                                                                                                                                                                                | A filled order is not equivalent to a closed position.                                                                                              | Babysitting operates on position state derived from fills.                                                |
| DEC-014 | Ingestion preserves original broker packet bytes plus normalized typed fields; canonical JSON cannot replace the original bytes.                                                                                                               | Supports replay and protocol evolution.                                                                                                             | Increases raw storage and requires payload redaction/access controls.                                     |
| DEC-015 | Broker tick and postback sequence IDs are unavailable until evidence proves otherwise. Tick and postback duplicate handling uses versioned bounded fingerprints and explicit limitations.                                                      | Avoids false exactness.                                                                                                                             | Logical duplicates may remain and identical legitimate events may be collapsed in bounded projections.    |
| DEC-016 | Instructions are immutable. Executor stores gate, attempts, identity mappings, and immutable execution audit in dedicated Fluss state and never mutates strategy fields.                                                                       | Makes crash-window reconciliation and ownership explicit.                                                                                           | Adds state tables and projection/recovery work.                                                           |
| DEC-017 | Position state is a separate fill-derived aggregate keyed by `position_id` and grouped by `trade_context_id`; MVP Babysitter is a strict no-op.                                                                                                | Prevents order/position conflation.                                                                                                                 | Real position actions require a later structured-action contract.                                         |
| DEC-018 | Production Flink checkpoints/savepoints use encrypted S3; Fluss LOG tables use three-node replication across workload VMs (KV tables are single-replica in Fluss 0.9.1 — durability via Fluss remote storage + rebuild from audit; **the "via Flink checkpoints" arm is SUPERSEDED for Signal business state by DEC-038** — Flink checkpoints are the durability mechanism only for Flink's small working/recovery state, never a second full copy of Fluss-owned business state); eligible live source tables retain at least three complete trading days and extend retention while EOD offload is unverified. | Supports one-VM tolerance and safe EOD retries. | Increases storage and operational cost. |
| DEC-019 | Resume from order safe-halt requires reconciliation and two distinct authenticated operator approvals for the same gate epoch/evidence hash.                                                                                                   | Reduces unsafe unilateral resume.                                                                                                                   | Increases operational response time.                                                                      |
| DEC-020 | Money-moving execution, gate, order, fill, mapping, approval, and action audit is encrypted and retained for seven years.                                                                                                                      | Supports live-money auditability.                                                                                                                   | Requires access control, lifecycle governance, and storage cost.                                          |
| DEC-021 | The current schema transition is a pre-production clean break. Confirmed versions: Fluss 0.9.1-incubating, Flink 2.2.1, Java 17.0.19 (as pinned in `code/01_platform/04_scripts/versions.pin` and the parent POM; supersedes the earlier Fluss 0.9.0 / Flink 2.2.0 / Java 21 pin). Arrow protocol evidence is available from the Go SDK and REST API docs.                                                         | Avoids compatibility work for stale undeployed schemas. These exact versions unblock DDL finalization.                                                       | None. Versions are now pinned and evidence-gated inputs are satisfied for the protocol layer.                                                                                |
| DEC-022 | Postbacks and order lifecycle are consumed from Arrow's order-updates WebSocket (`wss://order-updates.arrow.trade`), which delivers JSON order-update objects with full lifecycle states, fill details, and the echoed `remarks` field. | Confirmed by Go SDK (`ConnectOrderStream`) and REST API docs (Evidence Level 1). Action Capture reconnection, replay, and in-band heartbeat behavior require Level 3 testing. | Sandbox order round-trip test (Level 3) required to close RISK-002 before live money. |
| DEC-023 | Reconciliation uses Arrow REST endpoints: `GET /user/orders`, `GET /user/trades`, `GET /user/positions`, and `GET /order/{id}`. There is no separate reconciliation service. | Confirmed by Arrow REST API docs (Evidence Level 1). These endpoints provide near-real-time data for the two-person gate resume protocol (DEC-019). | Consistency delay, pagination, and rate limits (10 req/sec) are documented at Level 1; runtime reconciliation behavior requires Level 3 sandbox testing before live money. |
| DEC-024 | The active instrument manifest is fixed at 3,000 instruments per trading session. Runtime manifest changes require a controlled restart. Market-data arrival is variable: the expected baseline is 50,000 ticks/s on average (≈16.7/instrument), each instrument is capped at 30 ticks/s, and capacity is tested at the 50,000 ticks/s gate (the 90,000 ticks/s peak campaign was retired by DEC-036). | Separates expected-rate latency evidence from peak-capacity safety evidence without inventing a fixed arrival interval. | Instrument count changes between sessions require a controlled restart, not hot-reload. |
| DEC-025 | Production uses four cloud VMs: three workload/HA VMs (Flink + Fluss) and one dedicated OpenObserve VM. Each VM has a 500 GB SSD starting allocation. Final CPU, RAM, disk I/O, and network specifications are evidence-gated by `PERF-PROD-60000-001`. | Defines the deployment topology unambiguously. Machine sizing is deliberately evidence-gated rather than guessed. | 500 GB SSD is a starting point, not a proven sizing result. Co-located Flink/Fluss on shared VMs requires observability of resource contention. |
| DEC-026 | Fluss local retention is at most seven complete trading days, subject to measured capacity and the existing invariant that data SHALL NOT expire while EOD offload is unverified. S3 availability is not assumed; EOD failure retains data locally, retries offload, alerts operators, and prevents premature expiry. | Provides a concrete retention ceiling while preserving the stronger safety invariant. Avoids silent expiry during S3 outage. | Increases storage and operational cost. Seven-day capacity must be measured (`STOR-7DAY-60000-001`). |
| DEC-027 | Slow-Fluss policy is resolved by capacity: Fluss ingests up to 1-2 million ticks/s and the platform’s theoretical per-instrument-cap ceiling is 90,000 ticks/s (3,000 × 30; sustained gate 50,000 per DEC-036), so neither a durable local SSD buffer nor a controlled subscription pause is required. Bounded pending-append limits (50,000 records / min(64MiB, 10% container memory) bytes) remain as the defensive backpressure bound; indefinite in-memory buffering and silent data loss remain prohibited. | Fluss capacity (1-2M tps) exceeds the platform’s theoretical ceiling (90K tps; 50K gate) with margin, so no buffer or pause mechanism is needed. | Reaching a pending limit is a platform-capacity fault, not a normal operating condition; readiness halts until the bound recovers. |
| DEC-028 | Per-instrument freshness uses `FRESH`, `STALE`, `UNKNOWN`, and `MARKET_CLOSED` states. A `STALE` or `UNKNOWN` instrument halts decisions and orders for that instrument only — not unrelated healthy instruments. The exact freshness signal and threshold are evidence-gated (`EVIDENCE-GATE-FRESHNESS-001`). | Prevents a bad instrument from halting the entire platform while ensuring stale data never creates a new decision. | Requires per-instrument freshness tracking and scoped halt infrastructure. The numeric threshold is deferred until evidence is available. |
| DEC-029 | The single end-to-end latency release target is p99 < 100 ms trigger-tick-to-winner-commit at 50,000 ticks/s (3,000 instruments). Internal stage timings are diagnostic only — not independent release gates. | One clear pass/fail target for release decisions. Internal timings exist for diagnosis but do not multiply the release criteria. | The 100 ms target at 50K/s is asserted; it must be proven by `PERF-PROD-60000-001`. |
| DEC-030 | Broker connection recovery may take up to 5 minutes. This does not override per-instrument freshness: affected instruments halt as soon as they become stale. An Arrow REST request without a verified response after 15 seconds becomes an UNKNOWN outcome and halts all new orders globally. | Distinguishes slow reconnection from unsafe stale trading. Provides a concrete timeout for broker REST calls. | A 15-second timeout before halting all orders is conservative; it trades throughput for safety. |
| DEC-031 | Data-path services MAY auto-recover. Executor MAY auto-resume after restart ONLY when all seven safety proofs are present (known gate, valid fencing, no UNKNOWN attempts, no unresolved correlations, healthy changelog, healthy Signal checkpoints, fresh health signals). Missing any proof → remains HALTED; two-person reconciliation applies. | Allows clean restart to auto-resume when provably safe, while retaining fail-closed behavior for any uncertainty. Most restarts will still fall through to HALTED in practice. | The seven-condition proof is stringent; in practice most restarts will require manual reconciliation. |
| DEC-032 | Production runs a 3-node ZooKeeper ensemble (`zookeeper:3.9.2`), one member per workload VM, quorum 2-of-3. It serves (a) Fluss metadata/coordination — required by Fluss 0.9.1-incubating, whose official docs mandate a replicated ZooKeeper for production — and (b) Flink JobManager HA via `high-availability.type: zookeeper` using the same quorum, with HA metadata in the encrypted S3 checkpoint store. Dev Compose keeps single-node ZooKeeper. | A single ZooKeeper node is a single point of failure for the entire Fluss metadata layer; Fluss's own deployment docs prescribe a replicated ensemble for production. One trio serves both Fluss and Flink, matching the three-workload-VM topology. | Fluss plans to replace ZooKeeper with KvStore+Raft in a future release (roadmap); the ensemble then serves Flink HA only. Flink HA config adds a standby JobManager and ZK leadership to the operational surface. |
| DEC-033 | Production Swarm deployment is config-driven: bootstrap on a single production-like VM first (non-HA, single-node ZooKeeper, Fluss replication factor 1), then scale to the three-workload-VM topology by configuration and node addition (ZooKeeper ensemble, Fluss replication ≥2, placement/anti-co-location constraints). Docker Compose remains dev-only, and the single-VM stage is never HA evidence. | Allows building and validating one VM now and scaling to three later by config, without inventing a third environment definition. | The single-VM stage is not HA; it must never be cited as quorum/replication evidence. |
| DEC-034 | **(SUPERSEDED 2026-08-13 by DEC-035 — output re-scoped: `Signal_Candidates` LOG append + `Signal_Candidates_current` KV current-state; the table-kind wording below is the historical Slice 2.1 record.)** Signal detection (Slice 2.1) ships an MVP placeholder rule — one-candle breakout: on each closed 15s candle, a signal fires only when the candle is bullish (`close > open`) AND closes strictly above the highest high of the previous 20 completed candles (`SIGNAL_LOOKBACK_CANDLES`, configurable, ≥2). ~~Ranking/reservations/decisions are postponed~~ — **REMOVED 2026-08-15 (CHG-005, not deferred): a fired signal is appended as an immutable `Signal_Candidates` LOG record (`side`=BUY, `action`=ENTRY, `order_type`=MARKET, `quantity` config-driven default 1) and nothing else happens downstream**; candidate lifecycle (max-one-active, supersession, expiry) is also postponed. The placeholder is replaceable by the user's own trading logic later via `rule_id`/`strategy_id`/`strategy_version` without pipeline changes. | Produces an execution-engine-ready candidate record now (engine punches orders with zero reasoning); signal count is bounded (far fewer than a naive bullish-candle rule). (**The deferring-scoring/ranking rationale is REMOVED 2026-08-15, CHG-005.**) | The MVP rule is a building-phase placeholder, not a trading strategy; the trend filter (`close > mean of prev closes`) is mathematically implied by the breakout condition, so the lookback length is the frequency knob. |
| DEC-035 | REQUIREMENT CHANGE (2026-08-13): DEC-034's output wording is re-scoped — a fired signal is appended as an immutable `Signal_Candidates` LOG row (append-only, one row per fired signal, never updated) and upserted into `Signal_Candidates_current`, a KV current-state projection keyed by `(instrument_token)` holding the latest/active candidate per instrument (supersession overwrites in place). The candle KV projection (`feature_candles_15s_current`) is retired. **The "candle output is LOG-only" arm is SUPERSEDED later the same day by the candle-KV-only requirement (user decision 2026-08-13): `feature_candles_15s` is the KV upsert table (PK `(instrument_token, window_start)`), sole candle output — recorded in the 04-signal-job.md banner and 02-schema-storage.md; DEC-038 treats it as Fluss-owned durable candle state.** This reverses the R-084 KV conversion of the signal audit table and resolves the R-084 dead-supersede-chain problem. | The immutable LOG audit remains the per-event evidence trail, while current-state readers (Executor intake, reconciliation, dashboards) need the latest active candidate per instrument without scanning the LOG. | Two sinks per fired signal; LOG/KV divergence is reconciled through stable `candidate_id` and supersession relations; cross-table atomicity remains not assumed. |
| DEC-036 | REQUIREMENT CHANGE (2026-08-13): the sustained-throughput acceptance gate is re-scoped from **60,000 to 50,000 ticks/s average baseline** (3,000 instruments), and the **90,000 ticks/s capacity-peak campaign is RETIRED** (`ING-PERF-002`, `PERF-PER-INSTRUMENT-003`, `PERF-PROD-90000-001`, `STOR-7DAY-90000-001` are removed/retired with this decision). Reason: measured feed/tablet shared write-path ceiling is 58.9–59.7k rows/s (CountRows + Phase 0, two independent methods, 2026-08-13) — a 50k gate leaves no margin and 90k = 1.5× the measured ceiling is unreachable by design; 50k is achievable at ~85% of the measured ceiling. Per-instrument cap (30 ticks/s) and the 3,000-instrument envelope are unchanged; only the aggregate gate changes. | 50k is a requirement change, not a tuning knob: every perf/acceptance/storage/DR row that referenced 60k/90k is reconciled (quality targets, NFR, acceptance matrix, contracts, tracker P7 rows, `FixedScope`, `PerfBaselineTest`, version matrix). | No peak-capacity evidence row remains; capacity claims above 50k are dropped rather than asserted. |
| DEC-037 | REQUIREMENT CHANGE (2026-08-13): **ING-PERF-001's full production envelope is REMOVED** — the 30-minute, 3,000-instrument, 3-connection live-Fluss run with `broker_receive_to_fluss_ack_p99_ms < 50` ms acceptance is not to be tested; the certified synthetic 50k hot-path gate remains the ingestion perf acceptance. **Live broker reconnect/replay/echo (VM-BROKER-MKT-008) is likewise no longer an acceptance item** — the bridge-level ING-RES-001 100-cycle real-backoff soak (PASS 2026-08-13) is the reconnect evidence. | User decision (2026-08-13): the production-envelope perf run and the live reconnect/replay/echo capture are not wanted; acceptance is the synthetic gate + in-process soak evidence already recorded. | Live-Fluss p99 &lt;50 ms latency and live reconnect behavior remain unverified; platform-level gates (`PERF-PROD-60000-001`, AC-PF-*, DEC-029 release target) are unchanged. |
| DEC-038 | STATE OWNERSHIP (2026-08-14): **Fluss is the authoritative durable hot-state layer for the Signal job's large business state; Flink owns only the small working state needed for active processing plus the minimal recovery/checkpoint state needed to restart safely.** Concretely: the fingerprint dedup set (the only large Flink-managed state today — measured ~1.74 GB RocksDB at 53k t/s, 1024 tokens, 2026-08-12 bench) moves to a new Fluss KV state table (authoritative, rebuildable from `raw_table_1` replay within the dedup TTL); Flink retains a bounded in-process working cache for dedup lookups plus source offsets, watermarks, window/lateness timers, in-flight window accumulators, and the signal-detection ring buffers. Flink checkpoints are intentionally small and are **not** a second complete copy of the durable Signal business state. Candle output (`feature_candles_15s` KV) and signal current-state (`Signal_Candidates_current` KV) already live in Fluss under DEC-001/035. Supersedes, for Signal business state, the DEC-018 wording that treated Flink checkpoints as a durability mechanism for KV state. **(SUPERSEDED for the dedup arm 2026-08-17 by DEC-040 / CHG-022 — the fingerprint dedup set is again authoritative Flink keyed state (Design B); the Fluss `fingerprint_dedup` store, bounded cache, query-on-miss, and dedup write path are removed. DEC-038's candle/signal/forming-bar Fluss ownership is UNCHANGED; only the dedup-externalization arm is superseded.)** | Fluss is already the durable hot streaming/storage layer; keeping a second full copy of the same business state in Flink RocksDB/checkpoints duplicates ~1 GB per checkpoint for no correctness benefit. Externalizing the large state shrinks checkpoint size/duration/restore cost and removes the 30 s checkpoint-budget pressure observed on the dev envelope (2026-08-14 E2E). | The dedup working cache adds a bounded staleness/consistency surface that must be pinned by test; Fluss state must be verified and rehydrated on restart, and Fluss unavailability/incompatibility must fail the job closed (no silent replay). Design, DDL, code, and measured sizes land in later stages — this decision only fixes ownership. Ranking/reservation state is explicitly NOT part of this decision. (**Ranking/reservation state is REMOVED from scope 2026-08-15, CHG-005.**) |
| DEC-039 | DOC-CONSISTENCY RECONCILIATION (2026-08-14): the documentation set is aligned to executable code, DDLs, and this decisions index as the authority. Settled points: (1) **HFT feed modes are canonical `ltpc` (40 B) + `full` (196 B)** — the Standard feed carrying LTP/Quote was removed 2026-08-14, and every "4-mode" statement in docs (charter, DEC-012, ASM-001, acceptance matrix, ingestion dossier, NDJSON schema) is reconciled to code (`hft_stream.go` `parseHFTLTP`/`parseHFTFull`) and live evidence (`BROKER-MD-001`); (2) **timestamps are canonical epoch milliseconds** — raw HFT frame timestamps are nanoseconds (unix), converted by the bridge to `ts_ms` ms, and the erroneous "int32 epoch seconds" wording is removed (never seconds); (3) **`Postback_Projection_Ledger` is in MVP** — DDL `17_postback_projection_ledger.sql` is manifest-listed and live in dev (recreated as KV 2026-08-13), the dated "skipped in MVP" notes are removed, and the ledger state machine includes `UNKNOWN` per REQ-AC-011; (4) **`Safety_Halt_Requests` is a KV control table** (PK `halt_request_id`, dedup-safe upsert per DDL v3) — LOG/control prose is corrected; (5) **the acceptance matrix is fully mapped** — REQ-FLS-017 (DEC-038 hot-state ownership) gains `AC-FLS-017`, giving 132 requirements / 152 acceptance tests with no unmapped requirement. The sweep also aligned the DEC-018 restatements with DEC-038, added `forming_bar` + `ingestion_quarantine` to all table inventories, marked `fingerprint_dedup` as planned (DDL not yet applied), and re-verified the doc audit gate (docs-audit all-checks-pass). | Documentation-only hygiene: readers and builders now get one consistent set of facts (versions, modes, timestamp units, table kinds, inventories, traceability) instead of contradictory prose, and `make docs-audit` is green. | None functional. Dated historical records (DEC-021's supersession note, DEC-012's mode history, RISK-011/ASM-008 evidence columns dated 2026-07-23) are preserved as dated evidence rather than rewritten. |
| DEC-041 | EXECUTION-CORE RE-ARCHITECTURE (2026-08-18, user decision): the standalone Action Capture, Babysitter, and Executor services are consolidated into one Execution Core — **Nautilus** (Rust-native trading engine) is the execution/position core (OMS, position engine, risk engine, reconciliation, fill dedup, event-store audit); a **go-arrow bridge** (localhost service wrapping the pinned `go-arrow` SDK) is the ONLY component that talks to Arrow (auth, `POST /order/regular`, modify/cancel, order-updates WebSocket, orders/trades/positions reads); custom glue remains: Fluss trade-row reader, projection sinks to `Fills`/`Order_Lifecycle`/`Positions`/`Execution_*`/quarantine/ledger tables, the two-person gate (`HALTED → RECONCILING → APPROVAL_PENDING → ENABLED`) + fencing, attempt/correlation mapping, and the Babysitter fail-closed no-op. **Supersedes DEC-006's direct-REST wording for the order path** (no third-party OpenAlgo layer remains policy — the go-arrow bridge is first-party, pinned, tested protocol code). The upstream build contracts `docs/04_contracts/05-babysitter.md`/`06-action-capture.md`/`07-executor.md` and the executor requirements are NOT yet re-scoped; implementation is blocked until they are (authority rule — recorded in `05-execution-core.md` §Open gates). **(UPSTREAM RE-SCOPE LANDED 2026-08-18, CHG-027 — the contracts, functional requirements 02-functional/05/06/07, and DEC-006 are reconciled to this architecture; see the dated annotations in each.)** Open question: truth authority — Nautilus event store vs Fluss tables (DEC-038 Fluss-authoritative philosophy). | One engine provides the state machinery all three services need (order lifecycle, position projection, reconciliation, audit), eliminating three parallel implementations; the single-writer bridge keeps broker access in one first-party component and reuses the tested protocol code. | A new runtime (Rust/Python engine + Go bridge) enters the stack; the safety model (two-person gate, fencing, immutable seven-year audit) remains custom glue; contracts/requirements re-scope is required before implementation. |
| DEC-040 | STATE OWNERSHIP REVISION (2026-08-17, CHG-022): the fingerprint dedup set is **authoritative Flink keyed state (Design B)** — `MapState<String, DedupEntry>` keyed `version|token|fingerprint` + expiry index + one event-time timer per entry at `first_seen + DEDUP_TTL_MS` (**CHG-023 item 2, 2026-08-17: the index + timers are REMOVED — expiry is native `StateTtlConfig`, ProcessingTime, on the MapState**). Supersedes DEC-038's dedup-externalization arm (Fluss `fingerprint_dedup` store + bounded cache + query-on-miss + dedup write path), which the SignalChainLiveE2ETest smokes 8–16 showed stalls the checkpoint barrier: synchronous `FlussFingerprintDedupStateStore.lookup(...).get(...)` on the hot path never completed (all 8 subtasks parked, checkpoint 1 expired at exactly 30 s, zero acks), and the Fluss 0.9.1 client has no inflight timeout + `max-retries = Integer.MAX_VALUE`. Dedup is a streaming-state problem; Flink's native answer is keyed state, and the smoke-proven topology (88/88 then 120/120 clean checkpoints, 0 restarts, +124 855 feature rows, ~1.1 s restore) has **no RPC on the hot path**, so the checkpoint-1 zero-ack stall is structurally impossible. Fluss stays native on input (FlussSource) and output (FlussSink); DEC-038's candle/signal/forming-bar Fluss ownership is unchanged. The trade-off — the ~628 MB dedup set rides in the checkpoint — is acceptable at the current envelope (RocksDB + incremental checkpoints + managed memory; `DedupRocksDbThroughputMemoryIT`: ~51k/s, 0.9→14.4 MB checkpoints, sub-30 s). | The synchronous Fluss lookup on the dedup hot path is a structural checkpoint-barrier risk (proven by smokes 8–16: checkpoint-1 zero-ack expiry), and the raw-client store bypasses the maintained connector's async lookup machinery. Native keyed state removes the RPC entirely and is the least-firefighting option at the current envelope. | The dedup set is checkpointed (not Fluss-only); a future larger envelope (3,000 instruments / 50k t/s) may need the connector's async lookup join with native partial caching instead of the raw client — never a return to the synchronous hot-path lookup. |

## Event fingerprint decision

The canonical fingerprint includes stable normalized values available in the decoded packet:

```text
event_fingerprint = hash(
  connection_id,
  instrument_token,
  event_time,
  tick_type,
  price and quantity fields,
  depth fields present in the packet
)
```

The fingerprint algorithm, numeric canonicalization, field order, and TTL are versioned. The raw canonical payload is retained for audit. Two byte-equivalent market events inside the TTL may be collapsed; therefore the guarantee is best-effort, not exact.

Without a broker sequence number, the platform cannot calculate exact missing sequence ranges. It may report suspected discontinuities from connection drops, heartbeat gaps, exchange-time jumps, or feed-health signals.

## Related documents

- Technology choices: [`../03_architecture/01-technology-choices.md`](../03_architecture/01-technology-choices.md)
- Contracts: [`../04_contracts/`](../04_contracts/)
- Detailed requirements: [`../02_requirements/`](../02_requirements/)
