# C4 — SIG-PERF-001 50k baseline (bridge half) (2026-08-21)

Master-plan Task C4 — DoD: 50k baseline certified (bridge half; signal half stays pending).

**What was proven**

- `C4.1` — new `c4_sig_perf_test.go`:
  - `TestSigPerf50kSyntheticEnvelope` — 2,433×20 Hz = **48,660 tps** > 47,500 floor; per-token exact; envelope build < 100 ms.
  - `TestSigPerf50kSingleVsMulti` — N=1 vs N=3 no degradation.
- `C4.2` — bridge fan-out synthetic envelope PASS; Signal-job half (`PerfBaselineTest` live Fluss 10 s) stays TODO until D-era prod VMs — `04-signal-job.md` pending item NOT flipped (honest: synthetic only).
- `C4.3` — Go PASS 18.7 s, Rust 153/0.

**Disposition**

Envelope floor met at N=3 synthetic; the live-Fluss 50k gate (ING-PERF-001 / PerfBaselineTest) remains the production certification.

**Evidence**

- Change record: CHG-069.
- Files: `code/02_services/01_ingestion/go-bridge/c4_sig_perf_test.go`.
