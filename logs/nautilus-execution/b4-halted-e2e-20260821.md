# B4 — Signal→Intent HALTED-E2E (non-market half) (2026-08-21)

Master-plan Task B4 — DoD: signal → intent → fill round-trip green in sandbox (fill leg pending).

**What was proven (compose, live stack, 2026-08-21)**

- `B4SignalIntentE2ETest` (compute): with `EXECUTION_INTENT_ENABLED=true` → Signal_Candidates fires → immutable `Execution_Intent` canonical rows; default disabled → zero intents (fail-closed).
- `B4HaltedIntentConsumeDeferE2ETest` (gateway): intent → `DEFERRED` fail-closed, zero `Execution_Attempts`/`Order_Lifecycle` rows while the gate is HALTED.
- `B4.1` — pre-existing verified: `ExecutionIntentBuilder`/`ProducerFunction`/`FeedProtocol`/`TableColumns` — 18 `ExecutionIntent*` tests green; `EXECUTION_INTENT_ENABLED` defaults false (branch absent, fail-closed).

**Disposition**

Signal→intent verified on the live dev stack; intent→fill leg (attempts → Order_Lifecycle → Fills) still gated on broker sandbox (`BI-EQ ×1`, AC-paper-order A2→A3→A5). `B4.3` deferred to D-era.

**Evidence**

- Change record: CHG-087.
- Runner: `code/01_platform/04_scripts/run-b4-halted-e2e.sh`; sibling: `b4-signal-intent-e2e-20260821.md` (CHG-071).
